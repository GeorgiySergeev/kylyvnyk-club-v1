<!-- markdownlint-disable-file MD013 MD041 -->

ROLE: You are a senior software architect. Set up a production-grade monorepo for the "Kylyvnyk Club" project — an international business club platform.

CONTEXT:

- This is block 1 of 35. We are starting from an empty directory.
- The monorepo will host two Next.js apps (web, admin) and shared packages (ui, db, api, config, i18n).
- Stack: Next.js 15, React 19, TypeScript 5 strict, Tailwind v4, tRPC, Prisma, Supabase Postgres.

GOAL:
Initialize a pnpm + Turborepo workspace with the full folder skeleton, root configs, and working dev/build/lint/typecheck pipelines.

REQUIREMENTS:

1. Use pnpm@9 and turbo@2. Create `package.json` at root with:
   - "name": "kylyvnyk-club"
   - "private": true
   - "packageManager": "<pnpm@9.x>"
   - workspaces via `pnpm-workspace.yaml` pointing to `apps/*` and `packages/*`
   - scripts: dev, build, lint, typecheck, format, clean — all delegating to turbo
2. Create `turbo.json` with pipelines for: build (depends on ^build, outputs .next/**, dist/**), dev (cache: false, persistent: true), lint, typecheck, test, clean.
3. Folder structure (create empty `package.json` placeholders so workspaces resolve):
   - apps/web
   - apps/admin
   - packages/config
   - packages/ui
   - packages/db
   - packages/api
   - packages/i18n
4. Root files:
   - `.gitignore` (node_modules, .next, dist, .turbo, .env*, coverage, .DS_Store)
   - `.editorconfig` (utf-8, lf, 2 spaces, final newline)
   - `.nvmrc` with "20"
   - `.npmrc` with `auto-install-peers=true` and `shamefully-hoist=false`
   - `README.md` describing the architecture map (monorepo layout, each package's purpose, common commands)
5. Each placeholder package.json must have `"name": "@kyc/<folder>"`, version `0.0.0`, `private: true`, and empty `scripts` object.
6. Do NOT install Next.js, React, Prisma, tRPC, Tailwind yet — that happens in later blocks.

NAMESPACE: `@kyc/*` for all internal packages.

ACCEPTANCE CRITERIA:

- `pnpm install` runs without errors.
- `pnpm turbo run lint typecheck` succeeds (no-ops since nothing is implemented yet, but must not error).
- `ls apps packages` shows all 7 directories.
- README.md renders an accurate architecture map.

DON'Ts:

- Do not initialize git (assume user does it).
- Do not generate Next.js boilerplate in apps/web or apps/admin — leave them as empty placeholders.
- Do not add Husky, lint-staged, or commit hooks (handled in a later block if needed).
- Do not create any .ts/.tsx source files yet.

Output the full file tree you created at the end.
