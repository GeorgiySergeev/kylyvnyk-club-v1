ROLE: Senior fullstack engineer.

CONTEXT:
- Block 13 of 35. We now build the public catalog. The landing already links here with optional filters in the URL.
- Catalog must support SSR (SEO), cursor pagination (infinite scroll on mobile, "Load more" on desktop), filters (country, city, category, search), and Postgres full-text search.

GOAL:
Implement `partners.list` with filters + cursor pagination + Postgres FTS, build the catalog page with URL-state-driven filters and infinite scroll, and add the corresponding admin-side knobs for search later.

REQUIREMENTS:

1. **Postgres full-text search migration** (in `@kyc/db`):
   - Add a Prisma migration (or db push + manual SQL) creating:
     - A computed `searchVector` column on `Partner` of type tsvector. Use a SQL trigger or `GENERATED ALWAYS AS` expression combining `nameEn`, `nameRu`, `nameUk`, `descriptionEn`, `descriptionRu`, `descriptionUk`, `address`. Weight name fields A, descriptions B, address C. Use `simple` dictionary (multilingual-safe) — do not pick `russian` since we mix languages.
     - GIN index on `searchVector`.
   - Expose a helper in `@kyc/db/src/search.ts`:
     - `partnerSearchWhere(query)` returning a `Prisma.Sql` snippet usable in `prisma.$queryRaw` for ranking, OR
     - Better: a typed function that runs the query and returns `{ id, rank }[]` to be joined with `findMany` for type-safe results.
   - Add a `pg_trgm` extension migration so we can also do trigram-based "similar" matching as fallback when FTS returns nothing.
   - Update `prisma/seed.ts` to populate the column (it should auto-populate if GENERATED; otherwise run a refresh post-seed).
   - Add a test in @kyc/db verifying the search vector is non-null after seed.

2. **Implement `partners.list`** in `packages/api/src/routers/partners.ts`:
   - Input PartnerListInput (already defined):
     - `cursor?: string` (cuid)
     - `limit: number` default 24, max 60
     - `filters: { countryId?, cityId?, categoryIds?: string[], search?: string, status?: 'PUBLISHED' (locked for public), minDiscount?, sort?: 'newest'|'topRated'|'biggestDiscount'|'relevance' }`
     - `locale: 'ru' | 'en' | 'uk'` so we can prefer the right name in the response.
   - Behavior:
     - If `search` present → run raw FTS via @kyc/db helper, ranking + filtering. Fall back to trigram if rank=0.
     - Otherwise use Prisma `findMany`.
     - Always enforce `status = 'PUBLISHED'`, `deletedAt is null`.
     - Sort:
       - relevance (only when search given) → by FTS rank desc.
       - topRated → averageRating desc nulls last, reviewCount desc.
       - biggestDiscount → discountValue desc.
       - newest (default) → publishedAt desc.
     - Cursor pagination using `id` as tie-breaker.
   - Output: `{ items: PartnerListItem[], nextCursor: string | null, total?: number (only on first page; computed via separate count) }`.
   - PartnerListItem includes: id, slug, name (localized), shortDescription, logoUrl, city/country names (localized), categories (id + name localized), discountValue, discountText, averageRating, reviewCount.

3. **Implement `partners.bySlug`** (publicProcedure):
   - Returns full PartnerDetail (logo, cover, descriptions, address, lat/long, categories, country, city, discount, rating breakdown counts) — used in Block 14.
   - 404 via TRPCError NOT_FOUND if deletedAt set or status != PUBLISHED.

4. **Implement `partners.related`** (publicProcedure): given partnerId, return up to 6 other partners sharing at least one category in the same country.

5. **Catalog page** at `apps/web/src/app/[locale]/(marketing)/catalog/page.tsx`:
   - Server Component, reads searchParams: country, city, category (multi), search, sort, minDiscount, cursor.
   - Renders three things:
     - **FilterRail** (Client Component) — left sidebar on desktop, top sheet ("Filters" button) on mobile:
       - Country Select
       - City Select (dependent on country; resets if country changes)
       - Category multi-select (FilterPill chips with toggle)
       - Min discount slider (0–70)
       - Sort Select
       - "Clear all"
     - **SearchBar** (sticky on mobile) — top of grid, debounced (300ms), updates URL `?search=`.
     - **PartnerGrid** — server-rendered first page (calls tRPC server caller in the page); subsequent pages loaded via `useInfiniteQuery` on the client with `getNextPageParam: lastPage.nextCursor`. Uses `PartnerCard` from @kyc/ui.
     - Empty state with @kyc/ui EmptyState + suggested categories.
   - URL state management:
     - All filter changes update searchParams via `useRouter().replace(...)` shallow.
     - Server reads the same params for SSR.
   - "Filters" badge shows count of active filters.

6. **Catalog metadata**:
   - generateMetadata reflects filters in title/description ("Каталог партнёров — Юридические услуги в Киеве"). Build the title from active filters; fall back to a generic catalog title.
   - Canonical URL strips noisy params (only country/city/category/search), keeps order stable.
   - Add JSON-LD `ItemList` (first page) for SEO.

7. **Performance / UX**:
   - First page SSR'd to be SEO-friendly and instant.
   - Skeleton cards on subsequent infinite-scroll loads.
   - Use `Suspense` boundaries around the grid; FilterRail loads independently.
   - Image priority only on the first 6 cards.

8. **Locale-aware sorting/search**:
   - The Postgres FTS uses 'simple' so all three languages search equally well — verify with a Vitest test that a Russian query matches a partner whose RU name contains the term.

9. **Tests**:
   - Vitest: partners.list — basic filter combos, FTS hit + rank ordering, cursor pagination consistency (no duplicates, no skips), minDiscount filter.
   - Vitest: empty search returns full set; whitespace-only search treated as no search.
   - Playwright (or fixme spec): /ru/catalog → set country=UA, category=Юридические услуги, type "lex" → first card is Lex Pro.

10. **Admin search later** (note): in Block 31 we wire admin synonyms (`SearchSynonym` table) — add a TODO comment in @kyc/db schema where the table would go but DO NOT create it now. Avoid scope creep.

11. Update `apps/web/README.md` with a "Catalog" section: filter URL contract, FTS overview, how to add a new sort option.

ACCEPTANCE CRITERIA:
- /ru/catalog renders 24 seeded partners.
- Filtering by country reduces the list correctly; city select reacts to country change.
- Searching "lex" returns Lex Pro at rank 1.
- Infinite scroll loads next page without duplicates.
- All filters are reflected in the URL and shareable (paste URL in fresh tab → identical state).
- generateMetadata uses active filters.
- Lighthouse on a populated /catalog: SEO 100, A11y ≥ 95.
- Typecheck + lint + tests green.

DON'Ts:
- Do not implement client-side filtering — server is source of truth.
- Do not skip Suspense around the grid; it must stream.
- Do not store search history server-side (privacy).
- Do not include unpublished or soft-deleted partners.
- Do not couple search input to a tRPC call per keystroke — debounce + URL update only.
