<!-- Block 9. Placeholder. -->
ROLE: Senior Next.js engineer.

CONTEXT:
- Block 9 of 35. Phase 0 is complete: @kyc/config, @kyc/db, @kyc/api, @kyc/ui, @kyc/i18n are in place.
- Now we bootstrap apps/web — the public site + member dashboard.
- Stack: Next.js 15 App Router, React 19, TypeScript strict, next-intl (locale-prefixed routing), Tailwind v4 (via @kyc/ui globals), Serwist for PWA, tRPC client wired to @kyc/api.

GOAL:
Create a runnable Next.js 15 app at apps/web with: locale-prefixed routes (/ru, /en, /uk), tRPC + TanStack Query provider, PWA manifest + service worker, premium fonts (Cormorant Garamond + Manrope), a placeholder root page, and a working /api/trpc/[trpc] route handler that mounts the appRouter from @kyc/api.

REQUIREMENTS:

1. `apps/web/package.json`:
   - name `@kyc/web`, private, type "module"
   - scripts: dev (next dev --port 3000), build, start, lint (biome check), typecheck
   - deps: next@15, react@19, react-dom@19, next-intl@latest, @tanstack/react-query@^5, @trpc/client@^11, @trpc/react-query@^11, @trpc/server@^11, superjson, zod, next-auth@5 (beta or stable v5), @auth/prisma-adapter, @kyc/api workspace:*, @kyc/db workspace:*, @kyc/ui workspace:*, @kyc/i18n workspace:*, @kyc/config workspace:*, serwist@latest, @serwist/next@latest, lucide-react, framer-motion, sonner
   - devDeps: typescript@5, @types/node, @types/react, @types/react-dom, @biomejs/biome, autoprefixer
   - tsconfig extends `@kyc/config/tsconfig/nextjs.json`

2. `apps/web/next.config.ts`:
   - withSerwist wrapper (precaching off in dev, on in prod).
   - experimental: { typedRoutes: true, optimizePackageImports: ['lucide-react'] }
   - images.remotePatterns: allow Supabase storage placeholder, uploadthing.com, utfs.io.
   - transpilePackages: ['@kyc/ui', '@kyc/api', '@kyc/db', '@kyc/i18n', '@kyc/config'].
   - Wrap with `createNextIntlPlugin('./src/i18n/request.ts')`.

3. `apps/web/biome.json` extends `@kyc/config/biome.json`.

4. Folder layout:
   apps/web/
   ├─ public/
   │   ├─ icons/ (192, 256, 384, 512 + maskable)
   │   ├─ splash/ (basic iOS splash placeholders)
   │   ├─ manifest.webmanifest
   │   └─ robots.txt
   ├─ src/
   │   ├─ app/
   │   │   ├─ [locale]/
   │   │   │   ├─ layout.tsx
   │   │   │   ├─ page.tsx        (temporary "KYLYVNYK CLUB" placeholder)
   │   │   │   └─ not-found.tsx
   │   │   ├─ api/
   │   │   │   └─ trpc/[trpc]/route.ts
   │   │   ├─ ~offline/page.tsx   (PWA offline fallback)
   │   │   ├─ layout.tsx          (root HTML)
   │   │   ├─ global-error.tsx
   │   │   └─ favicon.ico
   │   ├─ i18n/
   │   │   ├─ request.ts          (next-intl getRequestConfig)
   │   │   ├─ routing.ts          (defineRouting from @kyc/i18n config)
   │   │   └─ navigation.ts       (createSharedPathnamesNavigation export)
   │   ├─ middleware.ts           (next-intl middleware; later extended with auth)
   │   ├─ providers/
   │   │   ├─ trpc-provider.tsx   ("use client" — QueryClient + httpBatchLink + superjson)
   │   │   ├─ theme-provider.tsx  (dark by default — class strategy)
   │   │   └─ app-providers.tsx   (composes Trpc + Theme + Toaster + CookieConsent)
   │   ├─ lib/
   │   │   ├─ trpc/
   │   │   │   ├─ client.ts       (createTRPCReact<AppRouter>())
   │   │   │   ├─ server.ts       (server-side caller via @trpc/server)
   │   │   │   └─ context.ts      (createContext for Route Handler)
   │   │   ├─ env.ts              (zod-validated process.env wrapper)
   │   │   └─ fonts.ts            (next/font: Cormorant Garamond display, Manrope sans, exposed as CSS vars --font-display, --font-sans)
   │   └─ styles/
   │       └─ globals.css         (imports @kyc/ui/styles/globals.css + app-specific overrides)
   └─ serwist.config.ts (or inline in next.config.ts) if needed

5. `src/i18n/request.ts`:
   - Use `@kyc/i18n` getMessages(locale).
   - Validate locale against `locales` from @kyc/i18n config; notFound() if invalid.

6. `src/middleware.ts`:
   - Export next-intl's `createMiddleware({ locales, defaultLocale: 'ru', localePrefix: 'always' })`.
   - matcher: `/((?!api|_next|.*\\..*).*)`.

7. `src/app/layout.tsx`:
   - Minimal HTML root: `<html lang="ru" suppressHydrationWarning>` (server-detected via header is fine for now).
   - Apply font CSS variables on `<body className={`${display.variable} ${sans.variable}`}>`.
   - Render `{children}`.
   - Export `metadata`: title template "%s — KYLYVNYK CLUB", description from messages.

8. `src/app/[locale]/layout.tsx`:
   - Receives `params: { locale }`. Validate, then wrap children with `NextIntlClientProvider` and `AppProviders`.
   - Render `<Toaster>` from sonner (positioned bottom-right).
   - Mount the CookieConsent component from @kyc/ui via the providers.

9. `src/app/[locale]/page.tsx`:
   - Server Component placeholder using @kyc/ui `Logo`, `SectionHeader`, `GoldButton`. Show "KYLYVNYK CLUB" + "Coming soon" string from marketing messages.
   - This is throwaway content — Block 11 replaces it.

10. `src/app/api/trpc/[trpc]/route.ts`:
    - Use `fetchRequestHandler` from `@trpc/server/adapters/fetch`.
    - Build context using `createContext` from @kyc/api + Prisma client from @kyc/db. Session is null for now (auth wired in Block 10).
    - Export GET and POST handlers.
    - runtime: 'nodejs' (Prisma not yet Edge-compatible).

11. `src/providers/trpc-provider.tsx`:
    - QueryClient with sensible defaults: staleTime 60s, refetchOnWindowFocus false, retry 1.
    - `trpc.createClient` with `httpBatchLink` -> `/api/trpc`, superjson transformer, headers passthrough.
    - Persist nothing yet (Web Storage persister added later if needed).
    - Devtools mounted only in dev.

12. PWA via Serwist (`@serwist/next`):
    - `apps/web/src/app/sw.ts` (or `apps/web/sw.ts` as @serwist/next requires): precache defaultRevisionedUrls + runtime caching strategies (network-first for /api, stale-while-revalidate for static).
    - `public/manifest.webmanifest`:
      - name "Kylyvnyk Club", short_name "Kylyvnyk", start_url "/", display "standalone", background "#0E0E10", theme "#D4AF37", icons array, lang "ru".
    - `~offline/page.tsx`: simple branded "You are offline" screen using @kyc/ui PremiumCard + GoldButton (retry).
    - Disable Serwist in dev.

13. `src/lib/env.ts`:
    - Zod-validated env: DATABASE_URL, DIRECT_URL, NEXTAUTH_SECRET (placeholder for Block 10), NEXTAUTH_URL, UPLOADTHING_SECRET (placeholder), UPSTASH_REDIS_REST_URL/TOKEN (optional in dev), RESEND_API_KEY (placeholder).
    - Throws on import in dev/prod if required vars missing. Skips validation when SKIP_ENV_VALIDATION=1.

14. `apps/web/.env.example` with every var documented.

15. `apps/web/README.md`:
    - How to run, the routing model (locale prefix), how providers stack, how to add a new page, how PWA works.

ACCEPTANCE CRITERIA:
- `pnpm --filter @kyc/web dev` boots on http://localhost:3000 and redirects `/` → `/ru`.
- `/ru`, `/en`, `/uk` all render the placeholder with the correct locale strings from @kyc/i18n.
- `/api/trpc/categories.list` reaches the router (returns NOT_IMPLEMENTED — that's expected).
- Lighthouse PWA shows installable + manifest valid.
- `pnpm typecheck` and `pnpm lint` pass.

DON'Ts:
- Do not implement auth UI yet (Block 10).
- Do not hardcode strings in components — import from @kyc/i18n via useTranslations / getTranslations.
- Do not put Prisma calls in Client Components; only in Server Components or via tRPC.
- Do not set `runtime = 'edge'` on the tRPC route (Prisma blocks this).
- Do not bypass middleware locale handling with manual redirects.