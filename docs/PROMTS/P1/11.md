ROLE: Senior frontend engineer + UI implementer working pixel-aware to mockup #1.

CONTEXT:
- Block 11 of 35. apps/web has routing, providers, and auth.
- We now build the public landing — the screen the user sees before becoming a member. Reference: mockup #1 (KYLYVNYK CLUB, gold-on-black, hero with globe, "Получить клубную карту", "Стать участником", "Каталог партнёров", "Как это работает", "Найти партнёров" filters, "Популярные категории" 4x3 grid, "Почему выбирают нас", country flags row, footer).
- We also expose the three legal pages and a working cookie banner.

GOAL:
Implement the marketing landing as a Server Component composed of premium sections, plus public `/legal/{privacy,terms,cookies}` pages backed by @kyc/ui MDX templates. Wire categories + countries from real data (tRPC) so the homepage stays accurate as the admin edits content.

REQUIREMENTS:

1. Implement the previously-stubbed tRPC procedures needed by this page:
   - `categories.list` (publicProcedure): returns active categories ordered by displayOrder. Cache: revalidate 5 min via Next's `unstable_cache` wrapper inside the router or via TanStack Query staleTime.
   - `geo.listCountries`: returns active countries ordered by displayOrder.
   - `geo.listCities` (by countryId).
   - `partners.list` (publicProcedure, cursor pagination): full implementation for the "Популярные партнёры" preview — limit 6, status=PUBLISHED, ORDER BY publishedAt DESC. Filters all optional here, but full filter wiring lands in Block 13.
   - All inputs/outputs use the Zod schemas from Block 4.

2. Page composition at `apps/web/src/app/[locale]/(marketing)/page.tsx` (Server Component) — replace the placeholder. Sections, in order:
   1. **Header (sticky, transparent → solid on scroll)** — Logo, primary nav (smooth-scroll anchors: Каталог, Как это работает, Категории, Страны), LocaleSwitcher, "Войти" link, "Получить карту" GoldButton. Mobile: hamburger Sheet from @kyc/ui.
   2. **Hero** — full-bleed dark background with a subtle globe image (place `public/img/hero-globe.webp` placeholder + add a <picture> with srcset for 1x/2x). Headline "KYLYVNYK CLUB" in display font with gold gradient text, subhead "Международный бизнес-клуб", country chips row (USA · Europe · Canada · Switzerland · Ukraine · Moldova · Worldwide), two CTAs: GoldButton "Получить клубную карту" (→ /[locale]/apply) and outline "Стать участником" (→ /[locale]/register).
   3. **3-card CTA row** — "Получить клубную карту", "Стать участником", "Каталог партнёров" each in a PremiumCard with icon + subtitle + arrow. Tap targets full card.
   4. **"Как это работает"** — 3-column grid (Участники / Партнёры / Международная система), each with icon, title, 4-bullet list as in mockup. Uses SectionHeader.
   5. **"Найти партнёров" quick filter** — three Selects in a row (Country / City / Category) backed by tRPC data. Submitting routes to `/[locale]/catalog?country=...&city=...&category=...`. Below the row a wide GoldButton "Найти партнёров". On mobile becomes a vertical stack.
   6. **"Популярные категории"** — 4x3 grid of `CategoryIcon` tiles; each links to `/catalog?category=<slug>`. Use the 12 seeded categories.
   7. **"Популярные партнёры" preview** — horizontal scroller (snap-x) of 6 `PartnerCard`s (no favorite toggle for guests; clicking heart routes to /login). "Смотреть все" link → /catalog.
   8. **"Почему выбирают нас"** — 4 `StatTile`s in a row: Проверенные партнёры / Скидки от 3% до 70% / Международный охват / Премиум уровень сервиса. Use lucide icons (Shield, Percent, Globe2, Crown).
   9. **"Страны"** — row of CountryFlag chips. Each links to `/catalog?country=<code>`.
   10. **Footer** — three columns:
       - Brand + tagline
       - Links: Каталог, Стать партнёром, FAQ, Контакты
       - Legal: Privacy, Terms, Cookies, "Manage cookies" button (calls the consent reopen API from @kyc/ui).
       - Bottom strip: © 2026 Kylyvnyk Club. All rights reserved. Locale switcher.

3. Animations:
   - Section reveal via framer-motion `whileInView` (only client islands — wrap small "RevealOnScroll" client component to avoid converting whole sections).
   - GoldButton scale 1.02 on hover (already in @kyc/ui).
   - Hero headline subtle 0.6s fade + slide up on mount.

4. SEO / metadata:
   - generateMetadata returns title, description, openGraph, twitter — all localized.
   - Add JSON-LD Organization schema in the head: name, url, logo, sameAs (placeholders), contactPoint.
   - Add hreflang alternates for ru/en/uk.

5. Legal routes under `apps/web/src/app/[locale]/legal/`:
   - `privacy/page.tsx`, `terms/page.tsx`, `cookies/page.tsx`.
   - Each renders @kyc/ui `LegalPage` with the MDX content for the active locale (use `getLegalContent('privacy', locale)` etc).
   - Substitute placeholders ([[COMPANY_NAME]] = "Kylyvnyk Club International", [[CONTACT_EMAIL]] = env NEXT_PUBLIC_CONTACT_EMAIL, [[EFFECTIVE_DATE]] = build-time date).
   - generateMetadata + canonical + hreflang.

6. Cookie consent:
   - CookieConsentProvider already mounted in Block 9 — ensure the banner is rendered above the footer.
   - "Manage cookies" footer button calls provider's `reopen()`.
   - Wire that analytics (PostHog later) only initializes if `consent.analytics === true`. Add a `ConsentGate` component in apps/web/src/components for future use; do not import analytics here yet.

7. Header behavior:
   - Implement as a small client component "MarketingHeader" handling scroll-state.
   - All other sections stay Server Components.

8. Performance:
   - All images use next/image with `sizes` and `priority` only on hero.
   - Fonts: rely on next/font CSS variables from Block 9.
   - Avoid waterfalling: fetch categories + countries + popular partners in parallel via Promise.all in the page.

9. Localization:
   - Use `getTranslations('marketing')` in the Server Component.
   - All copy from `marketing.json` (Block 8). If a string is missing, add it — but do not invent new copy beyond what's in the mockup.

10. Tests:
    - Vitest snapshot for `MarketingHeader` (compact mode + scrolled mode).
    - Playwright spec `e2e/marketing.spec.ts` (we install Playwright later — for now, write the spec file but mark `test.fixme` if Playwright isn't yet installed): visits /ru, asserts hero text, clicks "Получить клубную карту" → /ru/apply.

ACCEPTANCE CRITERIA:
- /ru renders the full landing matching mockup #1 visually (premium dark + gold).
- /en, /uk render with translated copy and same layout.
- Quick-filter Selects load real countries/cities/categories from the DB; submitting builds the right URL (catalog page itself comes in Block 13).
- Lighthouse: Performance ≥ 90, SEO 100, A11y ≥ 95 on mobile emulation.
- /[locale]/legal/{privacy,terms,cookies} all render with the MDX content and locale switching works.
- Cookie banner appears on first visit, choice persists, "Manage cookies" reopens the drawer.
- Typecheck + lint + Vitest pass.

DON'Ts:
- Do not hardcode category or country lists — they come from tRPC.
- Do not block initial render on auth state.
- Do not add analytics/tracking yet (Block 34).
- Do not implement the catalog list/grid filters here — only the homepage quick-filter (which deep-links into the catalog page that exists as a placeholder for Block 13).
- Do not use `next/link` legacy API; use the typed routes from app router.