ROLE: Senior fullstack engineer.

CONTEXT:
- Block 14 of 35. Catalog list works; clicking a card routes to `/[locale]/catalog/[slug]`.
- Partner detail must include: gallery, description (localized), discount, conditions, address with Leaflet map, contact info, share, ratings + reviews list, related partners. Favorite toggle is visible but requires login (redirects to /login if unauth).

GOAL:
Build the partner detail page with Leaflet integration (deferred client-side load), reviews readout (writes happen from member dashboard in Block 19), and SEO-grade structured data.

REQUIREMENTS:

1. **Implement reviews readout** in `packages/api/src/routers/reviews.ts`:
   - `reviews.listByPartner` (publicProcedure):
     - Input { partnerId, cursor?, limit default 10 }
     - Returns approved reviews only (status=APPROVED), newest first.
     - Each item: id, rating, text (truncated server-side to 1000 chars), authorName ("Иван И." → first name + initial of last), createdAt.
     - Cursor pagination.
   - Aggregated `reviews.summaryByPartner`: returns `{ avg, count, distribution: { 1: n, 2: n, ... 5: n } }` (cached via tRPC + revalidate 5 min in router using a simple in-memory map keyed by partnerId — Block 31 will replace with proper cache).

2. **Page** at `apps/web/src/app/[locale]/(marketing)/catalog/[slug]/page.tsx`:
   - Server Component. Fetch in parallel: `partners.bySlug`, `reviews.summaryByPartner`, `reviews.listByPartner (first page)`, `partners.related`.
   - notFound() if partner missing.
   - Sections:
     1. **Header strip** — breadcrumbs (Главная / Каталог / Категория / Партнёр), share button (Web Share API or copy-link fallback), favorite IconButton (calls tRPC `favorites.toggle` if logged in; otherwise redirects to /login?callbackUrl=...).
     2. **Hero card** (PremiumCard) — logo (large), name, primary category badge, city+country, average rating with star row, DiscountBadge (big), "Условия скидки" expandable.
     3. **About** — localized description (rich text from descriptionRu/En/Uk; render via MDX-lite — accept plain Markdown via `react-markdown` with a strict allowlist).
     4. **Contacts & Location**:
        - Address text + phone (click-to-call) + website (target _blank, rel noopener) + email (mailto).
        - **Leaflet map** lazy-loaded:
          - Client island `<PartnerMap lat lng name address />`.
          - Use `react-leaflet` + leaflet CSS imported inside the island.
          - Marker with custom gold-tinted SVG (inlined).
          - Disable scrollWheelZoom by default; provide a "Open in Google Maps" link below.
          - If lat/lng missing → show address text only, no map.
          - SSR-safe: dynamic import with `ssr: false`.
     5. **Reviews** — summary block (large avg, total count, distribution bars). Below: list of approved reviews with "Load more". Empty state if no reviews. Above the list, a CTA card: "Вы покупали у этого партнёра? Оставьте отзыв в личном кабинете" (links to /[locale]/deals if logged in, else /login).
     6. **Related partners** — horizontal scroller of up to 6 PartnerCards.

3. **Favorites integration**:
   - Implement `favorites.toggle` (memberProcedure): input { partnerId }. Toggles row, returns `{ isFavorited: boolean }`.
   - Implement `favorites.list` (memberProcedure) — used in Block 18; stub-replace here so types are correct.
   - On detail page, the heart icon reads initial state from a parallel `favorites.isFavorited({ partnerId })` query (add this small procedure too).
   - On toggle (logged in): optimistic update via TanStack Query.

4. **Generate metadata**:
   - title: `${partner.name} — ${partner.city}`.
   - description: first 160 chars of localized description.
   - openGraph: image = coverUrl or logoUrl, type 'website'.
   - JSON-LD `LocalBusiness` schema: name, image, address (PostalAddress), geo (GeoCoordinates), telephone, url, aggregateRating (if reviewCount > 0), priceRange optional. Also include `Offer` snippet for the discount if discountValue > 0.
   - hreflang alternates for ru/en/uk slugs (same slug, just different locale prefix).

5. **Leaflet packaging**:
   - Install: leaflet, react-leaflet@^4, @types/leaflet.
   - Import leaflet CSS only inside the client island (not in globals).
   - Provide a small fix for Leaflet's default marker icons being broken under bundlers (manually set `L.Icon.Default.mergeOptions` with hosted assets or inline).

6. **Performance**:
   - Map component dynamically imported, `ssr: false`, with a static placeholder image (`/img/map-placeholder.webp`) shown until intersection-observer triggers load.
   - Reviews list paginated client-side (useInfiniteQuery) after initial SSR page.

7. **Tests**:
   - Vitest: reviews.summaryByPartner returns correct distribution for a seeded partner with mixed reviews (seed a few in db:seed).
   - Vitest: partners.bySlug returns NOT_FOUND for unpublished/soft-deleted.
   - Playwright fixme spec: navigate from /catalog → first card → detail page; assert breadcrumbs, average rating visible.

8. **Seed updates** in @kyc/db:
   - Add ~30 approved reviews distributed across 10 partners (rating 3–5 weighted toward 4–5).
   - Add 5 dummy promotions on 5 partners so Block 15 has data.
   - Ensure lat/lng populated on at least 10 partners (use plausible city centers for seeded cities).

ACCEPTANCE CRITERIA:
- /ru/catalog/lex-pro renders full detail with map, address, contacts, description, rating, reviews, related.
- /en and /uk variants render with localized strings.
- Favorite heart toggles when logged in; redirects when logged out.
- Map shows marker at correct lat/lng; "Open in Google Maps" link works.
- JSON-LD validates (use schema.org validator mentally — verify shape).
- Lighthouse SEO 100, A11y ≥ 95.
- Typecheck + lint + Vitest green.

DON'Ts:
- Do not render Leaflet on the server.
- Do not load Leaflet CSS globally.
- Do not show pending/rejected reviews on the public page.
- Do not let the favorite click bubble to the card link.
- Do not paginate reviews by clicking "Next" — use "Load more" pattern.
