<!-- Block 16. Placeholder. -->
ROLE: Senior frontend engineer + UX implementer working pixel-aware to mockup #2 (member dashboard).

CONTEXT:
- Block 16 of 35. Phases 0–1 complete: auth + middleware-protected `/(member)/*` route group, premium UI kit, i18n, tRPC.
- We now build the authenticated shell: a layout shared by every member screen with header (greeting, locale, notifications), bottom-nav (Главная / Каталог / Моя карта / Избранное / Профиль) and a reusable Onboarding checklist widget that surfaces on the dashboard until the user completes all steps.
- The onboarding checklist content is admin-editable (Block 31). Here we render whatever is in the DB.

GOAL:
Create the member layout, bottom-nav, header, and onboarding checklist plumbing (data + widget). Subsequent blocks fill in the individual screens.

REQUIREMENTS:

1. **Folder & routing**:
   - `apps/web/src/app/[locale]/(member)/layout.tsx` — Server Component, protected by middleware (already in place).
     - Verifies session, calls `auth()` server-side, redirects to `/login` if absent (defense in depth — middleware already does this).
     - Fetches in parallel via the server tRPC caller: `member.getDashboard()` (lightweight: name, avatar, memberCardId, unreadChatCount, unreadNotificationsCount, onboardingProgress), `onboarding.listSteps()`.
     - Wraps children with `MemberShellClient` (client island holding bottom-nav state) and provides initial data via React Context.
   - Stub routes for the upcoming blocks so the bottom-nav has valid hrefs (each just returns a placeholder Server Component "Coming in Block N"):
     - `(member)/dashboard/page.tsx` (replaces the placeholder created in Block 10)
     - `(member)/my-card/page.tsx`
     - `(member)/favorites/page.tsx`
     - `(member)/profile/page.tsx`
     - Note: `/catalog` (already public from Block 13) is reachable from the bottom-nav.

2. **Implement `member.getDashboard` and `onboarding.listSteps`** in @kyc/api (replace NOT_IMPLEMENTED):
   - `member.getDashboard` (memberProcedure): joins User + Member + counts (favorites count, deals count, unread chat). Returns:
     `{ user: { id, name, image, locale }, member: { id, memberCardId, status, countryCode, cityName }, counters: { unreadChat, unreadNotifications, favoritesCount, dealsCount, referralsCount } }`.
     Use efficient `prisma.member.findUnique({ include: { _count } })` patterns.
   - `member.getOnboardingProgress` (memberProcedure): returns `{ stepKey: completedAt | null }[]` computed by joining active OnboardingStep with the Member.onboardingProgress JSON.
   - `member.completeOnboardingStep` (memberProcedure): input { key }. Sets `onboardingProgress[key] = ISOString(now)` if the step exists and is active. Idempotent. Audit-logged.
   - `onboarding.listSteps` (publicProcedure but cached): returns active steps ordered by displayOrder.

3. **Member Header** (`apps/web/src/components/member/MemberHeader.tsx`, client):
   - Left: Avatar (fallback to gold-initial circle) + "Добро пожаловать, {firstName}" — pulled from initial data.
   - Right: NotificationsButton (bell icon with red dot if unreadChat + unreadNotifications > 0; opens a Sheet listing latest in-app items — for now empty state "Уведомлений пока нет"; real wiring lands in Blocks 23/29), LocaleSwitcher, Hamburger (opens a Sheet with profile shortcuts and "Sign out").
   - Sticky on scroll, premium dark surface, no shadow on top (matches mockup).

4. **Bottom Navigation** (use `@kyc/ui` `BottomNav`):
   - 5 items in order: Главная (Home, → /dashboard), Каталог (Briefcase, → /catalog), Моя карта (CreditCard, → /my-card), Избранное (Heart, → /favorites), Профиль (User, → /profile).
   - Active state uses gold accent.
   - Visible only ≤ md breakpoint; on ≥ lg we render a left vertical Sidebar with the same items + the user mini-card on top. Both reuse the same `MemberNavItems` array.

5. **Onboarding Checklist Widget** (`apps/web/src/features/onboarding/OnboardingChecklist.tsx`, client):
   - Compact card shown on the dashboard (Block 17 will mount it; here we ship the component) when at least one step is incomplete.
   - Header: "Начните с этих шагов" + progress bar (`X из N`).
   - Each step row: lucide check icon (filled gold when completed, ring when not), title (localized), short description, "Перейти →" link (uses step.ctaHref). Completed rows are subdued.
   - When a row's CTA is clicked we DO NOT auto-mark the step complete — completion happens naturally:
     - "Заполни профиль" → marked complete when user saves profile (Block 21).
     - "Сохрани первого партнёра" → marked complete on first favorites.toggle add (Block 18).
     - "Изучи каталог" → marked complete on first /catalog/[slug] view by a logged-in member (add a tracking call inside the partner detail Server Component when ctx.session exists — use a small effect-only client island `<TrackOnboardingStep stepKey="explore_catalog" />` that fires once per session).
     - "Пригласи друга" → marked complete on first successful referral signup (Block 20).
   - When all steps complete, widget auto-hides for the lifetime of the dashboard view; on next mount it stays hidden because progress is persisted. Add a "Скрыть навсегда" link as escape hatch (sets a `dismissed: true` marker in onboardingProgress).

6. **Notifications + chat unread integration (read only)**:
   - The dot indicator on the bell pulls from `counters.unreadChat + counters.unreadNotifications` returned by `getDashboard`. Real-time updates land in Block 23 (chat); for now the widget refetches on window focus.

7. **Localization**:
   - All strings via `member.json`, `onboarding.json`. Add any missing keys to all three locales — do not invent copy beyond what the mockup implies.

8. **Skeletons & errors**:
   - Provide `<MemberShellSkeleton>` for streaming Suspense.
   - Error boundary at the (member) segment: branded "Что-то пошло не так" card with retry.

9. **Tests**:
   - Vitest: `member.completeOnboardingStep` — idempotent, unknown key throws BAD_REQUEST, inactive step rejected.
   - Vitest: `member.getDashboard` — correct counter shape with seeded data.
   - Playwright fixme spec: logged-in user sees bottom-nav + greeting + checklist with progress bar.

10. **Performance**:
    - `(member)/layout.tsx` renders header skeleton instantly; data flows via Suspense.
    - The shell is one of the only client islands; pages stay Server Components by default.

ACCEPTANCE CRITERIA:
- /ru/dashboard requires auth; visiting while logged out → /ru/login.
- Bottom-nav visible on mobile, sidebar on desktop ≥ lg, both navigate correctly.
- Header shows greeting with user's first name, bell with dot when counters > 0.
- Onboarding widget renders steps from DB and reflects completion correctly when triggered from other flows (verifiable: visit a partner detail page logged-in → "Изучи каталог" auto-completes after navigation).
- All locales render correctly.
- Typecheck + lint + Vitest green.

DON'Ts:
- Do not mark "Заполни профиль" complete merely on visiting /profile — completion fires from the profile form submit in Block 21.
- Do not duplicate the bottom-nav and sidebar in JSX — derive both from `MemberNavItems`.
- Do not put Prisma queries in Client Components.
- Do not block the page on the onboarding query — render the rest immediately, hydrate the widget in a Suspense boundary.