<!-- Block 23. Placeholder. -->
ROLE: Senior fullstack engineer with realtime experience.

CONTEXT:
- Block 23 of 35 (last of Phase 2). Members need a way to message a manager (1-to-1 thread per member, single Conversation row by design). Admin Inbox lives in Block 30.
- Stack: Supabase Realtime channels for live message delivery. We do NOT use Supabase Auth — only Postgres + Realtime over the same connection string. We use Supabase's anon key for the client realtime subscription, with Row Level Security (RLS) policies scoping which rows a member can see.
- Rate-limit: chatLimiter (30/min/user) — already in Block 5.

GOAL:
Implement chat procedures, a member-side chat widget (floating bubble + drawer), RLS policies on Conversation/Message, and Supabase Realtime subscription wiring with a graceful polling fallback.

REQUIREMENTS:

1. **Supabase Realtime + RLS setup**:
   - In @kyc/db, add a migration (raw SQL) that:
     - Enables RLS on `"Conversation"` and `"Message"`.
     - Adds Supabase auth-token-aware policies — but since we don't use Supabase Auth, we'll use a "JWT-claims-via-anon" pattern with a custom claim: the apps/web server issues a short-lived JWT (signed with SUPABASE_JWT_SECRET) containing `{ sub: userId, role: 'authenticated' }`, and the policies check `auth.uid() = "Message"."senderId" OR EXISTS conversation participation`.
     - Concretely, define a SQL function `app.current_user_id()` returning the `sub` claim from `auth.jwt()`.
     - Policies:
       - Conversation SELECT: memberId = app.current_user_id() OR EXISTS (User where id = app.current_user_id() AND role IN ('ADMIN','SUPER_ADMIN')).
       - Message SELECT: same membership rule via conversationId.
       - Message INSERT: senderId = app.current_user_id() AND conversationId belongs to caller (member side) OR caller is admin.
       - Conversation UPDATE: only admins.
     - Adds `ALTER PUBLICATION supabase_realtime ADD TABLE "Message", "Conversation";` so changes are broadcast.
   - Document the entire migration in @kyc/db README ("Realtime + RLS").

2. **Server JWT minter for Supabase**:
   - `apps/web/src/server/supabase/realtime-token.ts`:
     - `mintRealtimeToken(userId): { token: string; expiresAt: Date }` — HS256 with `SUPABASE_JWT_SECRET`, payload `{ sub: userId, role: 'authenticated', exp: now+10m }`.
   - Env: SUPABASE_URL, SUPABASE_ANON_KEY (PUBLIC, used client-side), SUPABASE_JWT_SECRET. Update .env.example + env.ts.

3. **Implement procedures** (replace NOT_IMPLEMENTED):
   - `chat.getOrCreateConversation` (memberProcedure):
     - Finds or creates the single Conversation for the caller (memberId = session.member.id).
     - Returns `{ conversation, realtimeToken, supabaseUrl, supabaseAnonKey }`.
     - Reset memberUnread to 0 in this call only if requested via input `markAsRead: boolean`.
   - `chat.listMessages` (memberProcedure):
     - Input { conversationId, cursor?, limit default 30, max 100 }.
     - Validates ownership.
     - Returns messages ordered desc (newest first) with cursor pagination on createdAt+id.
     - Each message: { id, senderId, senderRole (MEMBER|ADMIN|SUPER_ADMIN), body, attachments, createdAt, readAt }.
   - `chat.sendMessage` (memberProcedure, chatLimiter):
     - Input { conversationId, body (1-4000 chars), attachments?: Array<{ url, mime, sizeBytes, name }> }.
     - Inserts Message with senderId = current user.
     - Updates Conversation { lastMessageAt: now, unreadByAdmin += 1, status: 'OPEN' }.
     - Best-effort: also emit a notification to assigned admin (push later — for now just DB row).
   - `chat.markRead` (memberProcedure):
     - Input { conversationId }. Sets `unreadByMember=0` and marks all admin-authored Messages.readAt=now where readAt IS NULL in that conversation.

4. **Chat widget** (`apps/web/src/features/chat/ChatWidget.tsx`, client island):
   - Mount inside `(member)/layout.tsx` so it's present on every authed page.
   - Floating bubble fixed at bottom-right (offset above mobile bottom-nav by 96px on small screens).
   - Bubble shows unread count badge from `member.getDashboard.counters.unreadChat`.
   - Click → opens a Sheet (mobile bottom-up) / Popover (desktop right-aligned drawer ~420px width).
   - Inside:
     - Header: "Поддержка KYLYVNYK CLUB" + assigned operator name (if assigned) + close button.
     - Message list (virtualized via @tanstack/react-virtual — install) loading via `chat.listMessages` (first page server-prefetched on first open).
     - Composer: textarea (Cmd/Ctrl+Enter to send), attach button (UploadThing chatAttachment endpoint — add to UT router with limit 8MB / 3 files / member-only auth), send button.
     - Typing indicator (Realtime broadcast — see below).
     - Read receipts: small "Прочитано" under last own message if all subsequent admin messages have readAt set (well: only relevant inverse — when admin reads ours).
   - On open: calls `chat.getOrCreateConversation({ markAsRead: true })`, subscribes to Realtime channel.

5. **Realtime subscription**:
   - `apps/web/src/features/chat/useRealtimeMessages.ts`:
     - Creates Supabase client with `createClient(SUPABASE_URL, SUPABASE_ANON_KEY, { realtime: { params: { eventsPerSecond: 10 } }, global: { headers: { Authorization: `Bearer ${realtimeToken}` } } })`.
     - Subscribes to `postgres_changes` on the `"Message"` table filtered by `conversation_id=eq.${conversationId}`.
     - On INSERT: prepend to local query cache (TanStack), play subtle ping sound (gated by user pref), bump unread if widget closed.
     - On UPDATE (readAt set by admin): update local message.
     - Renew realtime token at 9 min interval (tokens expire in 10m).
     - Cleanup on unmount.
     - Polling fallback: if Realtime subscription enters `CHANNEL_ERROR`/`TIMED_OUT` 3 times within 30s, fall back to polling `chat.listMessages` every 8s; revert to Realtime once a fresh subscription succeeds.

6. **Typing indicators (optional but cheap)**:
   - Use Supabase Realtime `broadcast` channel `conversation:{id}:typing` to send `{ userId, isTyping }` events from composer onChange (debounced 800ms).
   - Render "Менеджер печатает..." when admin emits typing.

7. **Push hook**:
   - When `chat.sendMessage` server-side detects assigned admin offline (no recent activity heuristic — we don't yet have presence; defer real check to Block 30). For now, push to admin is best-effort: lookup admin PushSubscriptions with channel `chat=true` and dispatch via `sendPushToUser`. Wrap in try/catch.
   - Similarly, when admin sends a message (Block 30), it should push to member if member's chat channel enabled — add the matching dispatch in this block's `chat.sendMessage` so admin->member direction works once admin UI is in place. Use senderRole to decide direction.

8. **Onboarding step (optional)**:
   - No new step in MVP for chat.

9. **Localization**:
   - `chat.json` — add typing label, "Send message", "Прикрепить файл", "Сообщения переписки шифруются и доступны только вам и операторам клуба", read-receipt labels.

10. **A11y**:
    - ChatWidget bubble has accessible name "Open support chat".
    - Sheet/Popover traps focus, returns to bubble on close.
    - Messages list role="log" aria-live="polite".

11. **Tests**:
    - Vitest: `chat.sendMessage` rate limit (>30/min/user → TOO_MANY_REQUESTS).
    - Vitest: `chat.markRead` only touches admin-authored messages and only in own conversation.
    - Vitest: realtime token mint+verify roundtrip.
    - Playwright fixme: member opens widget → sends "Hello" → message appears in list immediately (optimistic), persists after reload.

ACCEPTANCE CRITERIA:
- /ru/* member surface shows the floating chat bubble with unread badge.
- Opening creates conversation; messages send via tRPC.
- Realtime delivery works when admin (manual SQL insert simulating admin reply) inserts a Message — message appears within ~1s in the member widget.
- Polling fallback engaged when Realtime fails (verify by blocking WebSocket in DevTools).
- RLS policies pass: in Supabase SQL editor, a token for another user can't SELECT this conversation.
- Typecheck + lint + Vitest green.

DON'Ts:
- Do not expose SUPABASE_JWT_SECRET client-side.
- Do not store attachments in the DB — only URLs from UploadThing.
- Do not skip RLS — without it, Realtime would broadcast every change to everyone.
- Do not poll faster than 8s in fallback; do not hold WebSocket if widget closed for > 60s of inactivity (disconnect, reconnect on open).
- Do not auto-open the widget — user must click.