<!-- Block 18. Placeholder. -->
ROLE: Senior fullstack engineer.

CONTEXT:
- Block 18 of 35. The dashboard is live; the partner detail page already has a heart that toggles favorites (Block 14 implemented `favorites.toggle` and `favorites.isFavorited`).
- Now we build the dedicated /favorites screen and ensure favorite state is synchronised across the app (catalog cards, detail page, dashboard rails).

GOAL:
Implement `favorites.list`, build the favorites page (grid + filters + empty state), and finalize optimistic-update behavior on `favorites.toggle` everywhere.

REQUIREMENTS:

1. **Implement `favorites.list`** (memberProcedure):
   - Input { cursor?, limit default 24, max 60, filters: { countryId?, categoryIds?[], search?, sort? } }.
   - Returns favorites joined with partner data, partner.status='PUBLISHED', partner.deletedAt is null. If a favorited partner has been unpublished/deleted, exclude it but DO NOT delete the Favorite row (preserve history).
   - Sort options: 'recently_added' (default — by Favorite.createdAt desc), 'name' (localized), 'biggestDiscount'.
   - Cursor pagination via Favorite.id.
   - Output shape mirrors `partners.list` items (so `PartnerCard` is reusable) PLUS `favoritedAt: Date`.

2. **Page** at `apps/web/src/app/[locale]/(member)/favorites/page.tsx` (Server Component):
   - Server-fetch first page using server tRPC caller.
   - URL state: `?country=...&category=...&search=...&sort=...&cursor=...`. Same conventions as catalog.
   - Layout:
     - Header strip: "Избранное" title + total count badge.
     - Compact filter row (Country Select, Category multi-select FilterPills, Search input, Sort select). Reuse `<FilterRail>` in a compact horizontal variant — refactor it from Block 13 into `<CatalogFilters variant="full" | "compact" />` so both pages share code.
     - Grid of PartnerCards (same as catalog).
     - Empty state: when zero favorites → branded EmptyState card with Heart icon, message "Пока ничего не сохранили", GoldButton "Перейти в каталог" → /catalog.
     - Infinite scroll via `useInfiniteQuery`.
   - generateMetadata: title "Избранное", noindex (private surface).

3. **`favorites.toggle` polish** (already implemented in Block 14 — extend here):
   - Server: on first successful add for a member, also call internal helper `markOnboardingStep(memberId, 'save_first_partner')` which sets the onboarding key.
   - Server: AuditLog action 'favorite.add' / 'favorite.remove' (low priority — fire-and-forget).
   - Client: shared mutation hook `useToggleFavoriteMutation()` in `apps/web/src/features/favorites/useToggleFavoriteMutation.ts`:
     - Optimistic update across all queries that contain the partner (catalog list, favorites list, partner detail, dashboard rail). Use TanStack Query's `queryClient.setQueriesData` with predicate matching `['partners.list', ...]`, `['favorites.list', ...]`, `['partners.bySlug', ...]`.
     - Snapshot prev state, rollback on error.
     - On success, invalidate `favorites.list` and any `favorites.isFavorited` queries to converge.
     - Debounce: prevent rapid toggle spam (300ms).

4. **`PartnerCard` favorite UX update**:
   - Heart icon button always reads state from a small `<FavoriteHeart partnerId>` client island so it can subscribe to the cache without making the whole card a client component.
   - For logged-out users on public catalog: clicking heart → redirect to `/login?callbackUrl=...&intent=favorite&partnerId=...`. On login completion, the login page reads `intent` and queues a favorite toggle then redirects to callbackUrl.

5. **Login intent handling**:
   - In `apps/web/src/app/[locale]/(auth)/login/page.tsx` (Block 10): after successful sign-in, inspect `intent` / `partnerId` searchParams, perform the queued `favorites.toggle`, then navigate.

6. **Counts**:
   - `member.getDashboard.counters.favoritesCount` should already include favorites count — update the underlying query to use `_count.favorites`.

7. **Localization**:
   - Add to member.json / common.json any missing strings: "Избранное", "Пока ничего не сохранили", "Перейти в каталог", "Недавно добавленные", "По названию", "По размеру скидки", "Партнёр временно недоступен".
   - When a favorite's partner is unpublished/deleted, list shows it greyed-out with "Партнёр временно недоступен"? No — we excluded it server-side per spec. Add the localized string just for future Audit/admin use; not used on this page.

8. **Tests**:
   - Vitest: `favorites.list` excludes unpublished/soft-deleted partners; sort+filter behave correctly.
   - Vitest: `favorites.toggle` marks onboarding step on first add only (call twice on a clean member → step set once).
   - Playwright fixme spec: logged in user clicks heart on /catalog → /favorites shows it → toggle heart again → empty state.

ACCEPTANCE CRITERIA:
- /ru/favorites lists saved partners with filters + sort + infinite scroll.
- Optimistic toggle is consistent across catalog/detail/favorites/dashboard.
- First favorite add for a new member auto-completes "save_first_partner" onboarding step.
- Empty state renders with CTA when no favorites.
- Soft-deleted partners disappear from the list but Favorite rows remain.
- Typecheck + lint + Vitest green.

DON'Ts:
- Do not hard-delete Favorite rows when partner is unpublished.
- Do not show favorite hearts on the marketing public landing — only on /catalog and partner detail and member surfaces.
- Do not optimistically update without rollback logic.
- Do not block the page on the `isFavorited` query per card — derive from list data.