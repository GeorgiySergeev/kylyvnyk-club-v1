<!-- Block 22. Placeholder. -->
ROLE: Senior fullstack engineer.

CONTEXT:
- Block 22 of 35. Events are admin-managed (CRUD in Block 29) and appear in the member area only (not public in MVP — easier; we can promote public later).
- Each Event has slug, localized title/description, cover image, startsAt/endsAt, locationName/cityId/address, capacity, status. EventRegistration ties member to event.

GOAL:
Build /events list, /events/[slug] detail, registration/cancellation flow for members. Use admin-PUBLISHED events only.

REQUIREMENTS:

1. **Implement procedures**:
   - `events.list` (memberProcedure):
     - Input { cursor?, limit default 20, max 60, filter: 'upcoming'|'past'|'mine'|'all' default 'upcoming', cityId? }.
     - Returns PUBLISHED events:
       - upcoming: endsAt >= now ORDER BY startsAt asc.
       - past: endsAt < now ORDER BY startsAt desc.
       - mine: only events the caller is registered to (joins EventRegistration).
     - Output items include: id, slug, title (localized), coverUrl, startsAt, endsAt, locationName, cityName (localized), countryCode, capacity, registeredCount (computed), isRegistered (for caller).
   - `events.bySlug` (memberProcedure):
     - PUBLISHED only. Full detail including descriptionLocalized, address, lat/lng (we'll reuse Leaflet map from Block 14 component).
     - Returns `{ event, isRegistered, registrationStatus: REGISTERED|CHECKED_IN|CANCELLED|null, registeredCount }`.
   - `events.register` (memberProcedure):
     - Input { eventId }.
     - Validates: event status=PUBLISHED, event.startsAt > now (cannot register after start), capacity not exceeded (use a transaction with FOR UPDATE-style lock — Prisma supports raw SQL or interactive transaction with `SERIALIZABLE`).
     - Upserts EventRegistration { status: REGISTERED }. If a previous CANCELLED registration exists for same (eventId, memberId), reactivate it.
     - On success, sends a Resend "Registration confirmed" email (template `event-registered.tsx`) and schedules a reminder push 24h before event start (record in a `ScheduledPush` table? No — out of MVP scope. Instead: store a `remindAt` timestamp on the registration row — add column `remindAt DateTime?` and Block 31 push-campaigns admin can target it later. For now: just send the immediate confirmation email; reminder dispatch will be implemented when a cron is in place. Add TODO.)
     - Audit-logged.
   - `events.cancelRegistration` (memberProcedure):
     - Input { eventId }. Sets status=CANCELLED if existed.
     - Sends "Registration cancelled" email.
   - `events.myEvents` (memberProcedure): convenience for dashboard — returns up to 3 upcoming registered events.

2. **Pages**:
   - **List** `apps/web/src/app/[locale]/(member)/events/page.tsx` (Server Component):
     - Tabs: Предстоящие / Мои / Прошедшие (URL `?filter=upcoming|mine|past`).
     - Grid of EventCard (new @kyc/ui component) — cover image, date badge (DD MMM), title, location, capacity progress bar (X/Y), "Зарегистрироваться" button or "Вы записаны" pill.
     - Filter row: city Select.
     - Infinite scroll via `useInfiniteQuery`.
     - Empty state per tab.
   - **Detail** `apps/web/src/app/[locale]/(member)/events/[slug]/page.tsx`:
     - Hero: cover image, title, date+time block, location, capacity progress.
     - Description (rendered from Markdown via react-markdown).
     - Map (reuse `<PartnerMap>` component renamed/extracted to `<GeoMap>` in apps/web — generalize props if needed).
     - Action card (sticky on desktop right column / floating bottom on mobile): "Зарегистрироваться" GoldButton or, if registered, "Отменить регистрацию" outline + "Добавить в календарь" link.
     - "Add to calendar" → builds an `.ics` file via a tiny server route `apps/web/src/app/[locale]/(member)/events/[slug]/calendar.ics/route.ts` (memberProcedure-authorized): returns text/calendar with VEVENT for the event.
     - Sharing: copy invitation link (deep link to the event detail).
     - Related events (3 upcoming in same city) at the bottom.

3. **Event registration UX**:
   - Optimistic toggle for `events.register` / `cancelRegistration` with rollback on error (capacity full → toast "К сожалению, мест больше нет").
   - When capacity reaches 100%, show "Мест нет" disabled button + waitlist hint (we don't implement waitlist MVP — just disable; add TODO).

4. **EventCard component** in @kyc/ui:
   - Premium dark card with overlaid date badge top-left, registration pill top-right when isRegistered, capacity progress bar bottom.
   - Hover lift micro-animation via framer-motion.
   - Storybook story.

5. **Email templates**:
   - `event-registered.tsx` — branded confirmation with event details + add-to-calendar link (web URL that downloads ICS).
   - `event-cancelled.tsx` — short cancellation confirmation.

6. **Dashboard integration**:
   - On dashboard (Block 17), add a "Мои события" mini-row above "Популярные партнёры" if `events.myEvents` returns 1+. Show up to 3 EventCards in compact variant.

7. **Localization**:
   - `events.json` namespace — populated in Block 8. Add capacity / waitlist / add-to-calendar strings if missing.

8. **Tests**:
   - Vitest: `events.register` — capacity guard (parallel registrations don't oversell — assert via simulated 5 concurrent calls on capacity=3 in a test transaction), can't register past event, reactivates CANCELLED row.
   - Vitest: `events.cancelRegistration` idempotent.
   - Vitest: ICS generator produces valid VCALENDAR with correct DTSTART/DTEND/SUMMARY/LOCATION.
   - Playwright fixme: member opens /events/{slug}, registers, button becomes "Отменить", calendar link downloads ICS.

ACCEPTANCE CRITERIA:
- /ru/events lists upcoming events; tabs filter correctly.
- /ru/events/{slug} shows full detail + map.
- Register / cancel works with capacity enforcement; emails sent.
- ICS calendar download works.
- Dashboard shows upcoming registered events when present.
- Typecheck + lint + Vitest green.

DON'Ts:
- Do not expose events as public (no SEO routes outside member shell yet).
- Do not allow registering for past or non-PUBLISHED events.
- Do not oversell capacity — wrap the check + insert in a transaction.
- Do not skip ICS validation on output (must parse in major clients).