<!-- Block 20. Placeholder. -->
ROLE: Senior fullstack engineer.

CONTEXT:
- Block 20 of 35. Dashboard tile "Рекомендовать клиента" → /(member)/referrals.
- Referral mechanics for MVP: each member has a unique code (auto-generated on first call). Sharing `?ref=CODE` to /register or to /apply creates a Referral row attributing the new member to the referrer once they convert.
- Attribution cookie: on landing with `?ref=CODE`, set `kyc_ref` cookie (90 days, http-only=false so client can read), respect existing if not expired.
- Conversion logic: Referral.status moves to COMPLETED when the referee becomes an active Member (status=ACTIVE, which happens on admin approval in Block 27). On COMPLETED, fire reward placeholder (we don't credit real money MVP; record `reward: 0` and emit AuditLog 'referral.completed' so admin can see).

GOAL:
Implement referral attribution end-to-end, build the /referrals page (code + share + stats + list), and wire cookie capture.

REQUIREMENTS:

1. **Code generation**:
   - Helper `apps/web/src/server/referrals/code.ts`:
     - `generateReferralCode(memberId): string` — base36 hash of memberId + random salt, length 8, uppercase, dash every 4 chars ("AB12-CD34"). Persist on Member as `referralCode` (add column to schema if missing — add migration: `Member.referralCode String @unique?`). Lazily create on first access.

2. **Implement procedures**:
   - `referrals.getMyCode` (memberProcedure):
     - Returns `{ code, shareUrl, qrToken: string (signed, short-lived), totals: { invitedCount, completedCount, pendingCount, totalReward } }`.
     - shareUrl = `${PUBLIC_SITE_URL}/${member.locale}/r/${code}`.
   - `referrals.stats` (memberProcedure):
     - Same totals + last-30-days breakdown by day (array of {date, invited, completed}) for a simple chart.
   - `referrals.list` (memberProcedure):
     - Paginated list of own Referral rows, with referee preview (initials + city + status). Cursor pagination.

3. **Attribution endpoints**:
   - New route `apps/web/src/app/[locale]/r/[code]/route.ts` (Route Handler):
     - Validates code (Prisma lookup; do not leak existence — always 302).
     - Sets cookie `kyc_ref` (90 days, sameSite lax, path /, NOT httpOnly so client SDKs can read).
     - Redirects to `/[locale]/apply?ref=${code}` (default) or `/[locale]/register?ref=${code}` if `?intent=register`.
   - Update `auth.register` (Block 10) and `applications.create` (Block 12) to read the `kyc_ref` cookie if not provided via input, and create a Referral row with status PENDING linked to:
     - on applications.create → Referral.refereeApplicationId? (skip — we don't have that FK). Instead store the code in `MembershipApplication.notes` or add a column `MembershipApplication.referralCode`. Add it: `referralCode String?` on MembershipApplication.
     - on auth.register → Referral.refereeId set to the new Member.id immediately if the member is created at registration. (Per Block 10 we DO create the Member row on register with status=PENDING, so Referral.refereeId is fillable.)
   - On admin "approve application" (future Block 27): if MembershipApplication.referralCode is present, locate the matching member's Referral row (or create one if direct register path wasn't used) and mark COMPLETED. Add a comment in this block's code TODO referencing Block 27.

4. **Page** at `apps/web/src/app/[locale]/(member)/referrals/page.tsx` (Server Component):
   - Server-fetch `referrals.getMyCode` + `referrals.stats` + first page of `referrals.list`.
   - Layout:
     1. Header: "Рекомендуйте клуб — получайте больше" + subtext (no money promised in MVP — say "Привилегии и статус" or similar non-binding copy).
     2. Hero card: big share URL chip (copy-to-clipboard), small QR (reuse `<MembershipQR value={shareUrl}>` component or a generic QR component — extract a `<QRCode value size />` shared component to `apps/web/src/components/QRCode.tsx` and refactor Block 17 to use it).
     3. Share buttons row: native Web Share API (mobile), Telegram, WhatsApp, Email, Copy link. Each uses a localized share-text template from `referrals.json` ("Присоединяйтесь к KYLYVNYK CLUB — закрытый бизнес-клуб с эксклюзивными скидками. Регистрация по моей ссылке: {url}").
     4. Stats tiles row: Invited / Pending / Completed / Total reward (display 0 — locked for MVP; small footnote "Reward program активируется позже").
     5. Sparkline / mini bar chart of last 30 days using `recharts` (install: `recharts`). Two bars per day: invited (gold) + completed (green).
     6. Recent invitees list — table on desktop / cards on mobile: avatar/initials, city, status badge, date. Pagination via "Load more".
     7. FAQ accordion (3 items): "Как работает реферальная программа?", "Когда защитывается приглашение?", "Что я получаю?".

5. **Onboarding step**:
   - Add onboarding step seed: key='invite_friend'. Implementation note: mark this step complete on the FIRST `referrals.list` row that transitions to status=COMPLETED. Since completion happens admin-side in Block 27, add a helper `markOnboardingStep(memberId, 'invite_friend')` that Block 27 will call. In this block, ensure the helper from Block 16 is reused and add a TODO comment.

6. **Cookie/share-text consent considerations**:
   - The `kyc_ref` cookie is "Functional" — gated by CookieConsent.functional (default ON unless user opts out via the consent banner). If consent.functional === false, do NOT set the cookie — still process `?ref=` query param at register/apply for the session only.

7. **Localization**:
   - `referrals.json` namespace — make sure all strings are present in RU/EN/UK including the share-text templates.

8. **Tests**:
   - Vitest: `generateReferralCode` produces unique stable codes; collisions handled (retry up to 3x).
   - Vitest: `/r/[code]` route sets cookie and redirects; invalid code still redirects without leaking.
   - Vitest: `applications.create` and `auth.register` pick up the cookie and create Referral row.
   - Vitest: Referral row's onboarding step is queued for trigger on completion (verify by simulating admin approval call).

ACCEPTANCE CRITERIA:
- /ru/referrals shows the user's code, share URL with copy + QR + share buttons, stats, chart, list, FAQ.
- Visiting `/r/AB12-CD34` redirects with cookie set; subsequent register/apply creates Referral row.
- Functional-consent OFF disables cookie path but session-only param still works.
- Typecheck + lint + Vitest green.

DON'Ts:
- Do not promise monetary rewards in MVP copy.
- Do not display real names of invitees — initials + city only (privacy).
- Do not allow self-referral (refereeId === referrerId).
- Do not let one applicant carry multiple referral codes — first-wins on cookie + query.