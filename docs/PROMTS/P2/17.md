<!-- Block 17. Placeholder. -->
ROLE: Senior frontend engineer + accessibility-aware UI implementer.

CONTEXT:
- Block 17 of 35. Member shell from Block 16 is mounted on /[locale]/(member)/*.
- Mockup #2 specifies the dashboard layout: large `MembershipCard` at the top (KYC-UA-000123, Ivan Ivanov, QR right), "Ваша скидка активна" status, "Показать карту" button opening a fullscreen QR; below it 4 action tiles (Каталог партнёров, Избранное, История сделок, Рекомендовать клиента), then the onboarding widget, then a "Найти партнёров" mini-filter and a "Популярные партнёры" rail with "Смотреть все".

GOAL:
Implement the member dashboard exactly per mockup #2, with a real QR backed by signed data, plus the fullscreen QR sheet.

REQUIREMENTS:

1. **Membership card QR payload**:
   - Server-side helper `apps/web/src/server/membership/card-token.ts`:
     - `createCardToken(memberId): { token: string; expiresAt: Date }` — signs `{ sub: memberCardId, iat, exp }` with HS256 using `MEMBERSHIP_CARD_SECRET` (env, 32+ chars). Default TTL: 90 days.
     - `verifyCardToken(token)` — used by future partner-side scanners; stub here for symmetry.
   - QR encodes a URL: `${PUBLIC_VERIFY_URL}/v/${token}` (PUBLIC_VERIFY_URL placeholder env, e.g. `https://kylyvnyk.club`). The page itself doesn't need to exist yet — partners scan it externally.
   - Add env vars to `.env.example` and `lib/env.ts`: MEMBERSHIP_CARD_SECRET, PUBLIC_VERIFY_URL.

2. **Implement `member.getCardData`** (memberProcedure):
   - Returns `{ memberCardId, fullName, status, countryCode, qr: { token, expiresAt }, gradient: 'gold' }`.
   - Calls `createCardToken` server-side. Token is generated per request — cheap.

3. **Dashboard page** at `apps/web/src/app/[locale]/(member)/dashboard/page.tsx` (Server Component):
   - Replace the placeholder. Use server tRPC caller to fetch `member.getCardData`, `partners.list({ limit: 6, sort: 'topRated' })`, `geo.listCountries`, `categories.list`.
   - Sections in order:
     1. **Header strip** — "Добро пожаловать, {firstName}" with date/time greeting variant ("Доброе утро/день/вечер" based on user locale + Intl).
     2. **MembershipCard** (large, hero) — uses @kyc/ui `MembershipCardSurface` with data fed in. Right-side QR area renders a `<MembershipQR token={...} size={104} />` client island.
     3. **Status row** — "Ваша скидка активна" with green dot, plus `GoldButton` "Показать карту" opening a fullscreen Sheet.
     4. **Action tiles 2x2 grid** — Каталог партнёров, Избранное, История сделок, Рекомендовать клиента, each a PremiumCard with lucide icon, subtitle from messages. All routes to the respective member route.
     5. **OnboardingChecklist** (from Block 16) — mounted here; hides itself when complete.
     6. **"Найти партнёров"** mini-filter — three Selects (country, city, category) + GoldButton; submitting deep-links to /catalog with params (reuse the same logic from the marketing landing — extract to `<QuickPartnerFilter />` shared component in `apps/web/src/components/partners/QuickPartnerFilter.tsx` and use it on both pages).
     7. **"Популярные партнёры"** — horizontal scroller of 6 PartnerCards with working favorite toggles (this is logged-in surface). "Смотреть все" → /catalog.
   - The dashboard is the only screen showing the QR + checklist + popular rail.

4. **`MembershipQR` client island** (`apps/web/src/features/membership/MembershipQR.tsx`):
   - Renders a QR code from `qrcode` package (install: `qrcode`, `@types/qrcode`).
   - Use canvas + a manual draw to allow a centered gold-tinted logomark (small KYC monogram).
   - Props: `value`, `size`, `inverted?: boolean` (white-on-dark vs dark-on-light — the small card-side QR uses dark-on-gold per mockup; the fullscreen sheet uses high-contrast dark-on-white for scannability).
   - SSR-safe: dynamic import the canvas draw inside a useEffect; render skeleton during hydration.

5. **Fullscreen "Show Card" Sheet** (`apps/web/src/features/membership/ShowCardSheet.tsx`, client):
   - Triggered by the "Показать карту" GoldButton.
   - Uses @kyc/ui Sheet (mobile bottom) / Dialog (desktop) — choose by `useMediaQuery`.
   - Content: header "Ваша клубная карта", large centered `MembershipCardSurface` with much bigger QR (240px) in high-contrast mode, member name + ID below, "Token expires {date}" footnote, "Close" button.
   - Locks scroll while open; ESC + click-outside close.
   - Refreshes the QR token if the open sheet detects token expiresAt within 5 minutes (calls `member.getCardData` again via TanStack Query refetch).
   - Wake-lock: when sheet open, request screen wake-lock via `navigator.wakeLock?.request('screen')` so the screen doesn't dim during partner scan. Release on close.

6. **Greeting time-of-day helper**:
   - `apps/web/src/lib/greeting.ts` — `getGreetingKey(hour, locale)` returns one of `morning|day|evening|night`. Pulled from message keys in member.json.

7. **Localization additions**:
   - Make sure member.json has: greeting variants, "Показать карту", "Ваша клубная карта", "Token expires", action tile titles + subtitles, "Популярные партнёры", "Смотреть все".

8. **A11y**:
   - QR has `role="img"` and `aria-label="QR code for membership card"`.
   - "Показать карту" button has `aria-haspopup="dialog"`.
   - Sheet traps focus, returns focus to opener on close.

9. **Tests**:
   - Vitest: `createCardToken` produces verifiable token; `verifyCardToken` rejects tampered tokens; expiration enforced.
   - Vitest: `getGreetingKey` returns correct bucket for boundaries (5h, 12h, 18h, 23h).
   - Playwright fixme spec: login → /dashboard → see card → click "Показать карту" → sheet opens with bigger QR; ESC closes.

10. **Performance**:
    - QR drawing happens once per token; memoize.
    - The popular-partners rail SSRs the first 6 cards.

ACCEPTANCE CRITERIA:
- /ru/dashboard matches mockup #2 in layout and content.
- Membership QR scans on phone to the verify URL containing the signed token.
- "Показать карту" sheet shows a high-contrast scannable QR; wake-lock acquired on supporting browsers.
- Onboarding widget hides itself when all steps are complete (verify by manually completing them).
- Typecheck + lint + Vitest green.

DON'Ts:
- Do not store the card token in the DB — it's stateless JWT.
- Do not render the QR on the server (canvas drawing is client-only).
- Do not include the user's email or phone inside the QR payload.
- Do not skip wake-lock release on unmount.
- Do not couple the popular-partners rail to the dashboard — reuse the catalog tRPC procedure.