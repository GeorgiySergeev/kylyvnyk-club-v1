<!-- Block 34. Placeholder. -->
ROLE: Senior frontend engineer focused on launch-readiness.

CONTEXT:
- Block 34 of 35. The product works end-to-end and is tested. Now we harden it for production: complete SEO surface, error monitoring (Sentry), product analytics (PostHog) with consent gating, PWA polish (offline, install-prompt, share-target), accessibility audit, and a launch checklist.

GOAL:
Ship every cross-cutting concern that turns "feature-complete code" into "ready to put in front of paying members".

REQUIREMENTS:

PART A — SEO:
1. **Sitemap + robots** (apps/web):
   - `apps/web/src/app/sitemap.ts` — generates sitemap including:
     - Static pages: /, /apply, /catalog, /legal/{privacy,terms,cookies} for each locale.
     - Dynamic: all PUBLISHED partners (`/catalog/[slug]`) with hreflang alternates for all 3 locales.
     - Categories landing pages (if we add them — for MVP skip; just list /catalog?category=<slug> equivalents as hreflang-only URLs is overkill; stick to canonical catalog).
   - `apps/web/src/app/robots.ts` — allows all, declares sitemap location, disallows /api, /dev, /(member).
   - Add per-locale `hreflang` alternates to every `generateMetadata` (block-11 landing, block-13 catalog, block-14 partner detail).

2. **Open Graph + Twitter cards**:
   - `apps/web/src/app/opengraph-image.tsx` — default OG image generated at runtime via `next/og` ImageResponse (Edge runtime). Premium dark+gold composition: logo + "KYLYVNYK CLUB" + tagline.
   - `apps/web/src/app/[locale]/(marketing)/catalog/[slug]/opengraph-image.tsx` — dynamic per-partner OG: partner logo, name, city, discount badge — also via ImageResponse.
   - Same pattern for `twitter-image.tsx` (or reuse the OG image — Twitter accepts og:image).
   - `metadataBase` configured in root layout (env `NEXT_PUBLIC_SITE_URL`).
   - For apps/admin: `noindex, nofollow` site-wide via `metadata.robots` in root layout; no OG.

3. **Structured data (JSON-LD)**:
   - Organization schema on landing (Block 11 already includes it — verify it's complete: name, url, logo, sameAs from env, contactPoint).
   - LocalBusiness on partner detail (Block 14 — verify completeness).
   - WebSite schema with SearchAction on /ru pointing at /catalog?search=.
   - ItemList on /catalog first page (Block 13 — verify).
   - BreadcrumbList on every nested page (catalog, partner detail, legal).

4. **Canonical URLs**:
   - Every page sets `canonical` to its locale-prefixed URL.
   - Catalog page canonical strips noisy params; only includes country/city/category/search/sort in a stable order.

5. **Performance budget**:
   - Add `next.config.ts` `experimental.optimizePackageImports` for the heavy deps (`lucide-react`, `date-fns`, `framer-motion`).
   - Audit images: every `<img>` replaced with `next/image` with `sizes`. Set `priority` only on hero.
   - Lighthouse target: Performance ≥ 90 mobile, ≥ 95 desktop.

PART B — SENTRY:
6. **Sentry setup** (both apps):
   - Install: `@sentry/nextjs`.
   - `apps/web/sentry.client.config.ts` and `sentry.server.config.ts` and `sentry.edge.config.ts` — DSN from env `NEXT_PUBLIC_SENTRY_DSN` + `SENTRY_DSN`, tracesSampleRate 0.1 (10%), profilesSampleRate 0.1, sendDefaultPii false, beforeSend filter that strips Authorization headers, cookies, member emails (apply `sanitizeForAudit` from Block 31 to event payloads too).
   - `apps/web/instrumentation.ts` — Next.js instrumentation hook registering Sentry.
   - Replay: enable Sentry Session Replay at 10% rate with `maskAllText: true`, `blockAllMedia: true`.
   - Source maps upload via `withSentryConfig` in next.config.ts; auth token in CI env `SENTRY_AUTH_TOKEN`, org + project envs.
   - Repeat for apps/admin with separate Sentry project.
   - tRPC integration: in @kyc/api, add an error-link/middleware that wraps procedure execution and reports unexpected errors (NOT user-facing TRPCErrors like UNAUTHORIZED/BAD_REQUEST) to Sentry with `setContext({ procedure, input (sanitized) })`. Add a small helper `reportError(error, ctx)` in @kyc/api/lib/observability.ts that both apps reuse.

7. **Sentry alerts (documented, not configured here)**:
   - In `docs/RUNBOOK.md`, list recommended alert rules: error spike, performance regression on /catalog, p95 > 2s on tRPC, Replay error sample.

PART C — POSTHOG (with consent gating):
8. **PostHog setup**:
   - Install: `posthog-js`, `posthog-node`.
   - Client: `apps/web/src/lib/analytics/posthog-client.ts` — lazy init only if `consent.analytics === true` (reads from CookieConsent hook from Block 7). Re-init on consent change event. Capture events only after init. Set `autocapture: false`, `capture_pageview: 'history_change'`, `person_profiles: 'identified_only'`.
   - Server: `apps/web/src/lib/analytics/posthog-server.ts` — Node client for server-side events (apply.submitted, application.approved, push.delivered, deal.confirmed). Identify by userId where applicable; flush at end of request.
   - Identify on login (Auth.js `signIn` event hook) with `{ distinctId: userId, role, locale, country }`. Reset on logout.
   - Wire a small `useTrack()` hook: `track(event, props)` is a no-op if consent off.
   - Apps/admin: separate PostHog project (or same project with `__internal: true` super-property to filter out staff data from product dashboards).

9. **Event catalog** (`apps/web/src/lib/analytics/events.ts`):
   - Typed const map of allowed event names + payload schemas (Zod). Use only these in code — prevents typos.
   - Events to track:
     - `view_home`, `view_catalog`, `view_partner_detail`, `view_legal`
     - `cta_clicked` (location, label)
     - `apply_started`, `apply_submitted`, `apply_failed`
     - `auth_register_completed`, `auth_signin_completed`, `auth_signin_failed`
     - `favorite_added`, `favorite_removed`
     - `partner_search` (term, resultsCount)
     - `partner_filter` (filters)
     - `deal_self_reported`
     - `review_submitted`
     - `referral_share_clicked` (channel)
     - `event_registered`, `event_cancelled`
     - `chat_opened`, `chat_message_sent`
     - `push_subscribed`, `push_unsubscribed`
     - `onboarding_step_completed`
   - Admin-side: `admin_application_approved`, `admin_partner_published`, `admin_review_approved`, `admin_push_campaign_sent`.

10. **Feature flags via PostHog (optional but cheap)**:
    - Expose `useFeatureFlag(key, defaultValue)` hook. No flags used in MVP — document the pattern in README. Server-side helper for SSR flag reading: `getServerFeatureFlag(key, fallback)` with timeout 200ms; falls back to default.

PART D — PWA HARDENING:
11. **Offline page** (Block 9 had a placeholder — flesh it out):
    - `/~offline/page.tsx`: branded screen showing cached membership card + favorites list + helpful copy. Pulls from IndexedDB.
12. **Service worker tuning** (Serwist runtime caching):
    - Static assets: `StaleWhileRevalidate`.
    - /api/trpc: `NetworkFirst` with 4s timeout.
    - Images from UploadThing: `CacheFirst` with 30-day expiration, max 200 entries.
    - Critical member-area routes (/dashboard, /favorites): runtime cached `NetworkFirst` so the last-seen page works offline.
    - Skip caching for /api/auth/* and /api/uploadthing/*.
13. **Install prompt UX**:
    - `apps/web/src/features/pwa/InstallPromptCard.tsx`: listens for `beforeinstallprompt`, surfaces a discreet card on the dashboard (only after 3 days since signup AND not yet installed) with "Установить как приложение" CTA.
    - Suppress for iOS Safari (no event there) — show iOS-specific instructions card on iOS by detecting userAgent and `'standalone' in navigator`.
14. **Share target**:
    - In `manifest.webmanifest`, declare `share_target` so users can share URLs into the app:
      - `action: "/share"`, method GET, params `{ title, text, url }`.
    - Route `apps/web/src/app/share/route.ts`: redirects to /catalog?search=<title or text> if param looks like a search query, otherwise to /catalog.
15. **Push hardening**:
    - On `notificationclick`, focus existing tab if open at the target URL; otherwise open new.
    - `notificationclose` event tracks dismissal (PostHog server event).

PART E — ACCESSIBILITY:
16. **A11y audit**:
    - Add `@axe-core/playwright` and run `await injectAxe(page); await checkA11y(page)` in 5 critical E2E specs (landing, apply, catalog, partner detail, dashboard). Allow level "moderate" failures (document in TESTING.md); block on "serious" + "critical".
    - Fix any criticals surfaced — typical: form labels, missing aria on icon-only buttons, color contrast on disabled states.
    - Add a "Skip to content" link in member + admin layouts.
    - Ensure focus rings (outline) are visible on all interactive elements; the dark+gold theme often hides them — add `:focus-visible` styles in globals.css with a gold ring.

PART F — RUNBOOK + LAUNCH CHECKLIST:
17. **`docs/RUNBOOK.md`**:
    - How to rotate secrets (NEXTAUTH_SECRET, MEMBERSHIP_CARD_SECRET, ADMIN_TOTP_ENCRYPTION_KEY, VAPID keys, Supabase keys).
    - Incident playbook: site down, Supabase pooler exhausted, Resend bounce spike, push delivery failures.
    - Backups: Supabase PITR setup steps; download a fresh backup monthly to cold storage.
    - Common queries (find orphaned files, count active members per country).
18. **`docs/LAUNCH_CHECKLIST.md`**:
    - DNS configured for kylyvnyk.club (web) + admin.kylyvnyk.club (admin).
    - SSL via Vercel automatic.
    - All required env vars set in Vercel for both projects (web + admin) for production + preview.
    - Supabase: production project + connection pooler URL; PITR enabled.
    - VAPID keys generated and saved.
    - Resend domain verified.
    - UploadThing app created.
    - Upstash Redis instance.
    - Sentry projects + alerts.
    - PostHog projects.
    - Initial super admin seeded; rotate password and 2FA enrolled immediately.
    - Cookie banner texts reviewed by legal.
    - Privacy / terms / cookies content reviewed with company name + jurisdiction filled in.
    - Lighthouse pass on /ru, /ru/catalog, /ru/dashboard.
    - All E2E specs green in CI.

19. **README updates** at root: link to RUNBOOK, LAUNCH_CHECKLIST, TESTING.

ACCEPTANCE CRITERIA:
- Lighthouse /ru, /ru/catalog: Perf ≥ 90 (mobile), SEO 100, A11y ≥ 95, PWA installable.
- Sentry events show up on a forced error in both apps.
- PostHog captures `apply_submitted` only when consent.analytics is true; toggle consent off → no further events.
- /share?text=lex redirects to /catalog?search=lex.
- /~offline page renders cached favorites when DevTools "Offline" enabled.
- axe-core checks pass on 5 spec pages.
- Lint + typecheck + Vitest + Playwright + Storybook test-runner all green.

DON'Ts:
- Do not ship PostHog without consent gating — would violate cookie banner contract.
- Do not log PII to Sentry — verify with manual test sending a tRPC error containing sample email; check Sentry event has email redacted.
- Do not cache /api/auth/* in the service worker.
- Do not generate OG images on every request without caching — `next/og` is fast, but rely on the route-segment cache (default 1y for static OG; for dynamic per-partner OG, use revalidate matching partner data tags).
- Do not enable PostHog session recordings without an explicit consent class added to the banner (we don't — keep replay disabled for MVP).