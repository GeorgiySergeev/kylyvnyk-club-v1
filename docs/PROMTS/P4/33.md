<!-- Block 33. Placeholder. -->
ROLE: Senior QA / test engineer.

CONTEXT:
- Block 33 of 35. The product is feature-complete. Throughout Blocks 5–32 we wrote scattered Vitest tests and fixme Playwright specs. Now we consolidate: unify Vitest config, build a Playwright E2E suite covering the critical-path user journeys (member + admin), complete Storybook coverage for every @kyc/ui component, and add a test database harness.

GOAL:
Standardize testing across the monorepo, write the missing E2E specs, ensure all Storybook stories pass interaction tests, and document the test pyramid + how to run each layer locally and in CI.

REQUIREMENTS:

1. **Test pyramid layout (document in `docs/TESTING.md`)**:
   - **Unit (Vitest)**: pure functions, Zod schemas, tRPC procedures with mocked Prisma — fast, no network.
   - **Integration (Vitest + real Prisma against a test DB)**: tRPC routers with real DB (Supabase test branch), real bcrypt, real cookies.
   - **Component (Storybook + @storybook/test-runner)**: every UI component has at least one story; critical ones (forms, MembershipCard, ChatWidget) have interaction tests.
   - **E2E (Playwright)**: full browser, real apps/web + apps/admin running in `pnpm dev`, real DB seeded into a known state.

2. **Vitest root config**:
   - `vitest.workspace.ts` at repo root listing workspaces: `packages/api`, `packages/db`, `packages/i18n`, `packages/ui`, `apps/web`, `apps/admin`.
   - Each project has its own `vitest.config.ts` extending a shared base at `packages/config/vitest/base.ts`:
     - environment: 'node' for packages/api, packages/db, packages/i18n; 'jsdom' for packages/ui, apps/web (component bits).
     - globals: false.
     - coverage: provider 'v8', reporter ['text','html','json-summary'], thresholds (lines 70 / functions 70 / statements 70 / branches 60 — these are realistic for an MVP; we can ratchet up later).
     - setupFiles per project for env loading + Prisma mocks.
   - Root scripts:
     - `pnpm test` → vitest run via workspace.
     - `pnpm test:watch` → vitest --watch.
     - `pnpm test:coverage` → vitest run --coverage.
     - `pnpm test:e2e` → playwright test.
     - `pnpm test:storybook` → @storybook/test-runner.

3. **Integration test DB harness** (`packages/db/test/`):
   - `setup.ts` — at file scope: read `TEST_DATABASE_URL` env, run `prisma migrate deploy` against it once, then expose `resetDb()` that TRUNCATEs all tables (in FK-safe order) and re-seeds a minimal fixture.
   - `fixtures.ts` — builders: `makeMember(overrides)`, `makePartner`, `makeCategory`, `makeAdmin`, `makeApplication`, `makeReview`, `makeDeal`. Each builder returns Prisma-ready data and resolves FKs lazily.
   - Tests using DB:
     - `beforeEach(resetDb)` for isolation.
     - Wrap groups in `describe.sequential(...)` to avoid parallel DB collisions, OR use a per-worker schema (better, but more setup — for MVP use sequential).
   - `.env.test` template documenting TEST_DATABASE_URL (a separate Supabase branch or dedicated local Postgres docker).
   - `pnpm db:test:reset` script that drops + recreates the schema + reseeds.

4. **Backfill unit tests** so each major procedure has coverage. For procedures already partially covered in Blocks 5/10/12/13/14/15/17/18/19/20/21/22/23/25/27/28/29/30/31/32, expand coverage to:
   - Happy path
   - Validation failure (Zod)
   - Authorization failure (publicProcedure called as anon → ok; memberProcedure called as anon → UNAUTHORIZED; adminProcedure called as MEMBER → FORBIDDEN; superAdminProcedure called as ADMIN → FORBIDDEN)
   - Rate-limit (where applicable)
   - Edge case (boundary, idempotency, concurrency where relevant)
   - Audit log written (where applicable)
   - Use the test DB harness for integration coverage; mocks for pure unit tests where the procedure has no DB dependency.

5. **Storybook coverage**:
   - Every component in `packages/ui/src/components/` MUST have a corresponding `*.stories.tsx` (this should mostly exist from Block 6 — fill the gaps now).
   - Add `play` interactions for: Button click toggles state, GoldButton emits onClick, PartnerCard heart toggle calls handler, PromotionCard copy chip writes to clipboard (mocked via `navigator.clipboard`), CookieConsent banner accept/reject persists cookie, OnboardingChecklist step click fires CTA.
   - Configure `.storybook/test-runner.ts` to run interaction tests headlessly in CI.

6. **Playwright setup**:
   - Install `@playwright/test`, `playwright`, `dotenv-cli` at root.
   - `playwright.config.ts` at root:
     - testDir: `e2e/`.
     - projects: chromium (mobile + desktop viewports), webkit (desktop), firefox (desktop).
     - webServer: starts `pnpm --filter @kyc/web dev` on :3000 AND `pnpm --filter @kyc/admin dev` on :3001 (use `webServer` array form available in Playwright 1.40+).
     - baseURL: http://localhost:3000.
     - retries: 1 in CI, 0 locally.
     - reporter: list + html.
     - globalSetup: seeds the test DB into a known state (calls `pnpm db:test:reset`).
   - `e2e/fixtures/`:
     - `auth.fixture.ts` — `login(page, role)` helper that uses storage state to bypass UI auth on most tests (one happy-path test does it via UI; rest reuse stored state).
     - `seed-data.ts` — references known seeded IDs (e.g., `LEX_PRO_SLUG`, `SUPER_ADMIN_EMAIL`).
   - `e2e/web/`: specs grouped by feature.
   - `e2e/admin/`: specs grouped by admin module.

7. **E2E specs to deliver** (replacing the fixme'd ones scattered throughout Blocks 11–32):
   - **web/marketing.spec.ts**: visit /ru → hero visible → click "Получить клубную карту" → /ru/apply.
   - **web/legal.spec.ts**: privacy/terms/cookies render in all 3 locales; cookie banner accept persists.
   - **web/apply.spec.ts**: submit application happy path → thanks page; submit again with same email → still thanks (dedupe); rate-limit after 5 attempts.
   - **web/catalog.spec.ts**: filters by country + category narrow results; search "lex" → Lex Pro first; infinite scroll loads next page without duplicates; URL is shareable.
   - **web/partner-detail.spec.ts**: open Lex Pro → JSON-LD present in head; map renders marker; favorites click while logged-out redirects to /login.
   - **web/auth.spec.ts**: register via UI → user lands on /login?registered=1; credentials login → /ru/dashboard; logout returns to /ru.
   - **web/member-dashboard.spec.ts**: logged-in user sees membership card with QR; click "Показать карту" → fullscreen sheet; bottom-nav navigates.
   - **web/favorites.spec.ts**: toggle heart on catalog → appears in /favorites → toggle off → empty state.
   - **web/deals.spec.ts**: self-report deal → appears as PENDING; member cannot review.
   - **web/referrals.spec.ts**: share URL contains code; visiting /r/CODE sets cookie; subsequent /apply persists referralCode.
   - **web/profile.spec.ts**: change name + upload avatar → reflected in header; account deletion flow signs out.
   - **web/events.spec.ts**: register for event → button flips; capacity-full event shows disabled state; cancel registration.
   - **web/chat.spec.ts**: open widget → send message → admin reply (simulated via direct tRPC call using admin session) → arrives in widget in < 3s.
   - **web/promotions.spec.ts**: hot deals visible on home; expired promotions excluded.
   - **admin/auth.spec.ts**: super admin signs in → 2FA prompt → enter test code → dashboard; admin without 2FA forced to /setup-2fa.
   - **admin/partners.spec.ts**: create partner → publish → appears on /catalog (cross-app navigation in same test using two contexts).
   - **admin/applications.spec.ts**: approve a NEW application → row moves; member appears in /members with memberCardId formatted KYC-XX-NNNNNN.
   - **admin/categories.spec.ts**: drag-reorder categories → /ru homepage shows new order.
   - **admin/promotions.spec.ts**: create + activate promotion → public /ru shows it.
   - **admin/events.spec.ts**: create + publish event → registers a member (via stored state) → admin sees registration → check-in via direct procedure (skip camera).
   - **admin/inbox.spec.ts**: admin opens conversation → replies → member context card shows correct stats.
   - **admin/reviews.spec.ts**: approve a PENDING review → public partner page shows it + updated rating.
   - **admin/audit.spec.ts**: audit page lists actions; diff drawer renders before/after.
   - **admin/dashboard.spec.ts**: KPI tiles render numbers; Cmd+K palette opens; navigate to /partners via palette.

8. **Mocking strategy**:
   - **Resend**: in tests, set `RESEND_API_KEY=mock-`. Add a tiny test transport that captures emails into an in-memory array; expose via `apps/web/src/server/email/__mocks__/resend.ts` and use `vi.mock` in unit tests; for E2E, the env value `mock-` flips the client to a logging stub. `e2e/helpers/email.ts` reads the captured emails via a dev-only `/api/dev/emails` route guarded by `NODE_ENV !== 'production'` for assertions.
   - **Resend dev route**: `apps/web/src/app/api/dev/emails/route.ts` — returns last 20 captured emails. Throw 404 unless NODE_ENV in {test, development}.
   - **Push (web-push)**: same pattern — env flag enables a capture array readable via `/api/dev/push`.
   - **UploadThing**: in tests, monkey-patch the file URL to a `data:` URL of a 1x1 PNG.
   - **Supabase Realtime**: in E2E we accept the small delay; the chat spec triggers the admin reply via tRPC directly to keep it deterministic.
   - **Stripe / payments**: not in MVP — skip.

9. **Stable test IDs**:
   - Add `data-testid` attributes to critical interactive elements across both apps (login form fields, primary CTAs, datatable rows, kebab menus, dialog buttons). Convention: `data-testid="<feature>.<element>"` e.g., `apply.submit`, `partners.row.<id>`, `chat.composer.send`. Update existing components to include them.

10. **Flakiness controls**:
    - Disable framer-motion animations in tests via env `NEXT_PUBLIC_DISABLE_ANIMATIONS=1`; in @kyc/ui, wrap `MotionConfig` reading this var with `reducedMotion="always"`.
    - Network idle waits: prefer `expect(locator).toHaveText(...)` over `waitForTimeout`.
    - Time mocking for countdown components: tests pass `now` prop to Countdown to avoid wall-clock flake.

11. **Coverage gates**:
    - Configure Vitest coverage threshold globally (70/70/70/60). CI fails below thresholds.
    - Coverage from Playwright is not collected (acceptable for MVP).

12. **README updates**:
    - Root `README.md` → add "Testing" section with one-line commands for each layer.
    - `docs/TESTING.md` (new) — full test strategy, fixtures, mocks, troubleshooting.

13. **Acceptance run**:
    - `pnpm test` — all Vitest workspaces green.
    - `pnpm test:coverage` — passes thresholds.
    - `pnpm test:storybook` — interaction tests pass.
    - `pnpm test:e2e` — full Playwright suite green (chromium first; webkit/firefox can be CI-only).

ACCEPTANCE CRITERIA:
- All test commands above pass locally on a freshly seeded test DB.
- At least one Playwright spec per feature listed above is present and passing on chromium.
- Storybook test-runner runs interaction tests with zero failures.
- docs/TESTING.md exists and matches the actual setup.
- Coverage report generated under `coverage/` at root.

DON'Ts:
- Do not couple E2E specs to a live external service — mock Resend, web-push, UploadThing.
- Do not use sleep/timeout in Playwright — use locators + auto-wait.
- Do not commit `playwright-report/`, `test-results/`, or coverage outputs to git (add to .gitignore).
- Do not run integration tests against the production DB — TEST_DATABASE_URL must be a distinct DB.
- Do not skip resetDb between tests — flakiness compounds quickly.