<!-- Block 25. Placeholder. -->
ROLE: Senior fullstack engineer + CMS UX designer.

CONTEXT:
- Block 25 of 35. Admin shell + 2FA in place. We now build the most-used admin module: Partners.
- Partner record drives the public catalog (Block 13/14). Must support: localized name/description (RU/EN/UK), logo + cover upload via UploadThing, category multi-assign, country/city, address with optional lat/lng, discount config, publish/unpublish, soft-delete + restore, full-text search rebuild on edit.
- Permissions: ADMIN can CRUD; SUPER_ADMIN additionally can hard-delete (purge) soft-deleted rows.

GOAL:
Implement all admin partner procedures, build the list page (DataTable with rich filters), the create/edit form, the soft-delete + restore + purge flows, and ensure search-vector + averageRating recompute hooks.

REQUIREMENTS:

1. **Implement procedures** in `packages/api/src/routers/admin/partners.ts`:
   - All wrapped in `auditedAdminProcedure(...)` (from Block 5) so every mutation hits AuditLog.
   - `admin.partners.list`:
     - Input: { cursor?, limit default 50, max 200, search?, status?: 'all'|'DRAFT'|'PUBLISHED'|'ARCHIVED'|'DELETED', countryId?, cityId?, categoryIds?[], sort?: 'newest'|'updated'|'name'|'discount', includeDeleted?: boolean }.
     - Returns rich rows for the table including: id, slug, primary name (RU), status, country/city names, categories (chips), discountValue, averageRating, reviewCount, deletedAt, updatedAt, createdById/updatedById attributions, publishedAt.
   - `admin.partners.get`: full row for the editor including all localized fields, full URLs for logo/cover, lat/lng, address.
   - `admin.partners.create`:
     - Input: PartnerCreateInput (from schemas, Block 4). All three locales required for name; descriptions can have at least one filled.
     - Validates: slug unique (auto-generate from nameRu via @kyc/api `lib/slug.ts`; collision → append -2, -3...).
     - On create: status defaults to DRAFT; searchVector auto-populated (generated column from Block 13).
     - Returns the created row.
   - `admin.partners.update`:
     - Input: { id, patch: PartnerUpdateInput }.
     - Updates fields, regenerates slug only if explicitly requested (toggle), bumps updatedAt.
     - If lat/lng changed, that's fine — no extra processing.
   - `admin.partners.setCategories`: { partnerId, categoryIds[] } — replaces M:N rows in a transaction.
   - `admin.partners.publish`: { id } — sets status='PUBLISHED', publishedAt=now (only if currently DRAFT|ARCHIVED). Throws if missing required fields (name in all 3 locales, at least one description, city, at least one category).
   - `admin.partners.unpublish`: { id } — sets status='ARCHIVED' (NOT DRAFT; keeps it distinct from never-published). Public site immediately stops showing it. Existing favorites remain (per Block 18 invariant).
   - `admin.partners.softDelete`: { id } — sets deletedAt=now, status='ARCHIVED'. Cascading is logical only — Favorite/Deal/Review rows untouched.
   - `admin.partners.restore`: { id } — clears deletedAt.
   - `admin.partners.purge` (superAdminProcedure): { id } — requires deletedAt set ≥ 30 days ago in production; in dev allow immediate. Performs hard delete with appropriate FK strategy:
     - Disallow purge if Deal rows exist (FK protection); admin must reassign or accept failure.
     - Otherwise: delete Reviews tied to those Deals (none if Deals==0), Favorites for this partner, Promotions, then Partner.
   - `admin.partners.recomputeRating`: { id } — recalculates averageRating + reviewCount from APPROVED reviews. Called internally on review approval (Block 31) and exposed for manual fix-ups.
   - `admin.partners.bulkUpdateStatus`: { ids[], status } — bulk publish/unpublish/archive.

2. **Slug helper** in `packages/api/src/lib/slug.ts`:
   - `slugify(input, locale)` — transliterates Cyrillic to Latin for RU/UK (use `transliteration` package or a small custom map), strips diacritics, lowercases, replaces non-alnum with `-`, collapses dashes.
   - `ensureUniqueSlug(prismaModelDelegate, base, existingId?)` — appends `-2`, `-3`... until unique.

3. **List page** at `apps/admin/src/app/[locale]/(admin)/partners/page.tsx` (Server Component):
   - Header: "Партнёры" + GoldButton "Добавить партнёра" → /partners/new.
   - DataTable (TanStack Table v8 wrapped):
     - Columns: Logo (small), Название (clickable → /partners/[id]), Статус (badge), Категории (chips, max 2 + "+N"), Страна/Город, Скидка, Рейтинг (avg + count), Обновлено (relative), Действия (kebab menu).
     - Sortable headers; server-side sort via tRPC `sort` param.
     - Toolbar: search input (debounced 300ms, server-side via `search` param), status segmented control (All / Draft / Published / Archived / Deleted), country filter, category multi-select.
     - Row actions kebab: "Редактировать", "Опубликовать"/"Снять с публикации" (context-aware), "Дублировать", "Удалить" (soft), "Восстановить" (if deletedAt set), "Удалить навсегда" (SUPER_ADMIN only, only if deletedAt set).
     - Bulk actions bar (when ≥ 1 row selected): "Опубликовать", "Снять", "Удалить".
     - Pagination with cursor — show "Загрузить ещё" button at bottom (DataTablePagination wrapper).
     - URL state for filters/sort/cursor (mirror catalog page conventions).
   - Empty state: gold CTA "Добавить первого партнёра".

4. **Create + Edit page**:
   - Single route handles both: `apps/admin/src/app/[locale]/(admin)/partners/[id]/page.tsx` with special id "new".
   - On "new": pre-fill empty form with country defaulting to admin's most-used country (skip optimization — leave empty). Submitting `admin.partners.create` then redirects to `/partners/{newId}`.
   - On existing: load via `admin.partners.get` server-side.
   - Layout: 2-column on desktop:
     - Left main column:
       - **Section "Основное"**: slug (read-only with regenerate toggle), I18nTabs with name + description per RU/EN/UK (TipTap or a simple `<Textarea>` for now — install `@tiptap/react @tiptap/starter-kit` and wrap as `<RichEditor>` in apps/admin/src/components/forms/RichEditor.tsx).
       - **Section "Локация"**: country Select, city Select (depends on country), address, lat/lng (numeric inputs with "Pick on map" button opening a small Leaflet picker dialog reusing @kyc/ui PartnerMap component generalized in Block 22). Lat/lng auto-fill on map click.
       - **Section "Скидка и условия"**: discountValue (number), discountText (localized — I18nTabs), "Условия использования" rich text per locale.
       - **Section "Контакты"**: phone, website, email.
       - **Section "Медиа"**: logo (UploadThing partnerLogo endpoint), cover (new "partnerCover" endpoint — add to UploadThing router, max 4MB).
     - Right sidebar (sticky):
       - **Status panel**: current status badge, primary button context-aware ("Опубликовать" / "Снять с публикации"), "Сохранить" button (gold), "Сохранить как черновик" outline.
       - **Категории**: multi-select Combobox with chips, drag to reorder (primary = first); calls `admin.partners.setCategories` on save.
       - **Метаданные**: createdAt, updatedAt, createdBy, updatedBy.
       - **Опасная зона** (collapsible): "Удалить" button → soft-delete; if already deleted, shows "Восстановить" + (SUPER_ADMIN only) "Удалить навсегда".
   - Form: RHF + Zod with the shared schemas. Autosave NOT enabled MVP (save explicitly). Show "Несохранённые изменения" indicator when dirty.
   - On Publish click: client validates required fields locally, otherwise server-validates and returns clear error messages — display inline.

5. **Image cleanup**:
   - When logo or cover is replaced or partner is purged, schedule UploadThing key deletion via `utapi.deleteFiles`. Wrap in try/catch — non-fatal.

6. **Review approval side-effect (placeholder)**:
   - When Block 31 implements review moderation, on approve it calls `admin.partners.recomputeRating`. Add an internal helper `recomputePartnerRating(partnerId, tx)` exported from the partners router so Block 31 can call it within its transaction.

7. **Cache invalidation strategy**:
   - On any partner mutation in admin, server-side invalidates the public catalog via Next.js `revalidateTag` — tag partner data with `partners:list`, `partners:slug:${slug}`, `partners:related`. Wrap `partners.list` and `partners.bySlug` in `unstable_cache` with these tags. Trade-off: adds caching now (good for perf); document in README.

8. **i18n**:
   - All UI strings via admin.json. Add any missing keys.
   - Field labels in 3 locales (admin-facing); the localized partner fields themselves are user-managed content.

9. **Tests**:
   - Vitest: slugify Russian "Лекс Про" → "leks-pro" (or similar transliterated); ensureUniqueSlug appends suffix.
   - Vitest: publish blocks when required fields missing.
   - Vitest: soft-delete sets deletedAt; restore clears; purge blocked when Deal rows exist.
   - Vitest: bulkUpdateStatus respects allowed transitions.
   - Vitest: recomputeRating from a mix of APPROVED + PENDING reviews counts only APPROVED.
   - Playwright fixme: create partner → upload logo → set 3 locales → publish → appears on /catalog.

10. **README** in `apps/admin/README.md` — section "Partners" describing the editor model + cache tags.

ACCEPTANCE CRITERIA:
- /ru/partners DataTable lists seeded partners with filters/sort/bulk actions.
- Create partner end-to-end: form valid → DRAFT row → Publish → public catalog reflects it within seconds (revalidateTag).
- Edit existing partner: changing nameEn updates the EN catalog page.
- Soft-delete hides from /catalog; Restore brings it back; Purge blocked if Deal rows exist.
- AuditLog records every mutation with actor + before/after.
- Lint + typecheck + Vitest green.

DON'Ts:
- Do not allow editing slug freely if PUBLISHED (avoid SEO breakage). Provide an "Изменить slug" toggle that warns about redirects (we'll handle redirects later — for MVP just allow with warning).
- Do not let DRAFT/ARCHIVED/DELETED partners surface in the public catalog under any code path.
- Do not delete Favorites/Deals on soft-delete.
- Do not skip cache invalidation — public pages will get stale.
- Do not let ADMIN purge — only SUPER_ADMIN.