<!-- Block 29. Placeholder. -->
ROLE: Senior fullstack engineer.

CONTEXT:
- Block 29 of 35. Members register for events (Block 22). Admin needs to CRUD events, see registrations, perform check-in (mark CHECKED_IN), capacity adjust, manual reminders dispatch, and cancel events.

GOAL:
Implement admin.events CRUD + registration management + manual reminder broadcast.

REQUIREMENTS:

1. **Implement procedures** in `packages/api/src/routers/admin/events.ts`:
   - `list`: { cursor?, limit, search?, status?: 'all'|'DRAFT'|'PUBLISHED'|'CANCELLED'|'COMPLETED', when?: 'upcoming'|'past'|'any', cityId? }.
   - `get`: full event including registeredCount, capacity, status.
   - `create`: input { slug?, titleRu/En/Uk, descriptionRu/En/Uk, coverUrl, startsAt, endsAt, locationName, cityId, address, lat?, lng?, capacity?, status: 'DRAFT' default }. Auto-slug from titleRu if absent.
   - `update`: { id, patch }. If capacity decreased below registeredCount, refuse with explicit error and suggested fix.
   - `publish`: { id } — sets PUBLISHED, validates required fields like partners.publish.
   - `cancel`: { id, reason } — sets CANCELLED; marks all REGISTERED rows as CANCELLED; sends `event-cancelled-admin.tsx` email to every registrant; revalidateTag('events:list').
   - `markCompleted`: { id } — sets COMPLETED (manual; in MVP we don't auto-complete).
   - `setCoverUrl`: { id, coverUrl }.
   - `delete` (superAdminProcedure): only allowed when DRAFT or CANCELLED with zero registrations.
   - `listRegistrations`: { eventId, cursor?, limit, status?: 'all'|'REGISTERED'|'CHECKED_IN'|'CANCELLED' }.
   - `checkInRegistration`: { eventId, registrationId } — sets status=CHECKED_IN, checkedInAt=now. Audit-logged.
   - `undoCheckIn`: same but reverts (audit-logged).
   - `addManualRegistration`: { eventId, memberCardId } — admin can register a member on their behalf (e.g., phone request). Validates capacity.
   - `cancelRegistration`: { eventId, registrationId } — sets CANCELLED + email.
   - `sendReminderNow`: { eventId, audience: 'REGISTERED'|'ALL_MEMBERS', subject?, body? } — sends a push + optional email to the audience. Push uses Block 21 `sendPushToUser` filtered by users with channel `events=true`. Email is optional via toggle. Rate-limited at admin scope (no more than 5 calls per event).

2. **Pages**:
   - **events/page.tsx**:
     - DataTable: cover thumb, title (RU), status badge, when (startsAt formatted + "через N дней" / "N дней назад"), location, capacity (X/Y bar), registeredCount, kebab.
     - Toolbar: search, status filter, when filter (upcoming/past/any), city filter.
     - Row actions: Edit, Publish/Cancel, Mark completed, Open public page, Duplicate.
   - **events/[id]/page.tsx**:
     - id="new" → blank form.
     - Tabs:
       - **Содержимое** — title/description per locale (I18nTabs), cover (UploadThing eventCover endpoint — add to UT router, max 6MB), startsAt/endsAt (datetime), location (name, cityId, address, lat/lng with map picker reusing the picker from partners), capacity, status segmented control DRAFT/PUBLISHED/CANCELLED/COMPLETED.
       - **Регистрации** — only shown for non-new events:
         - DataTable: member avatar, name, memberCardId, status, registeredAt, checkedInAt, kebab (Check in / Undo / Cancel).
         - Top: "Добавить вручную" button (Combobox by memberCardId or email) + bulk check-in toggle (scan member cards — opens a QR scanner page using browser BarcodeDetector or `@yudiel/react-qr-scanner` install; expected QR payload is the membership card verify URL; after scan, lookup memberCardId and check them in. Show success/failure toasts).
         - Export CSV button (server endpoint streams CSV of registrations).
       - **Напоминания** — small panel: composer (audience select, optional subject + body, "Отправить сейчас" button). Show log of past reminder sends with sentCount.

3. **QR check-in flow**:
   - Use camera-based scanner from `@yudiel/react-qr-scanner`. Mount only when user clicks "Сканировать QR". On detection, parse the URL, extract the JWT token (Block 17 format `/v/${token}`), call a new procedure `admin.events.checkInByToken({ eventId, token })` which verifies the JWT via `verifyCardToken` (Block 17), looks up the member, and check-ins.
   - Provide a "Manual entry" tab if camera is unavailable.

4. **CSV export endpoint**:
   - `apps/admin/src/app/[locale]/(admin)/events/[id]/registrations.csv/route.ts` — server-side stream (Node runtime), respects admin session, returns CSV: memberCardId, name, email, phone, status, registeredAt, checkedInAt.

5. **Cache invalidation**:
   - Wrap member-side `events.list` and `events.bySlug` in unstable_cache with tags `events:list`, `events:slug:${slug}`. Revalidate on relevant admin actions.

6. **Localization**:
   - `admin.json` + `events.json` — verify parity.

7. **Tests**:
   - Vitest: create rejects startsAt ≥ endsAt; publish requires required fields.
   - Vitest: cancel marks all registrations as CANCELLED + email queued.
   - Vitest: capacity decrease below registeredCount blocked.
   - Vitest: `checkInByToken` rejects tampered/expired tokens.
   - Vitest: CSV stream has correct columns + escapes commas/quotes.
   - Playwright fixme: create event → publish → registration appears in admin → QR scan check-in → status flips.

ACCEPTANCE CRITERIA:
- Admin can CRUD events; member side reflects changes.
- Registrations tab works including QR check-in flow.
- CSV export downloads correctly.
- Cancellation broadcasts to registrants.
- Lint + typecheck + Vitest green.

DON'Ts:
- Do not allow capacity reduction below current registeredCount.
- Do not bypass JWT verification on QR check-in.
- Do not duplicate registrations: enforce unique (eventId, memberId).
- Do not show DRAFT events on the member side.
- Do not allow ADMIN role to delete events with registrations — only SUPER_ADMIN after CANCEL.