<!-- Block 31. Placeholder. -->
ROLE: Senior fullstack engineer + ops UX designer.

CONTEXT:
- Block 31 of 35. Four related admin features bundled here because each is small individually:
  1. **Push campaigns**: broadcast push notifications to segments of members.
  2. **Onboarding checklist editor**: CRUD the OnboardingStep records that the member dashboard widget renders.
  3. **Review moderation**: approve/reject pending reviews; on approve, recompute Partner averageRating.
  4. **Audit log viewer**: searchable list of AuditLog rows.

GOAL:
Implement all four feature areas with one page each, end-to-end with tests.

REQUIREMENTS:

PART A — PUSH CAMPAIGNS:
1. **Procedures** in `packages/api/src/routers/admin/push.ts`:
   - `campaigns.list`: { cursor?, limit, status?: 'all'|'DRAFT'|'SCHEDULED'|'SENT'|'FAILED' }.
   - `campaigns.get`: full row.
   - `campaigns.create`: { title, body, url?, audience: AudienceFilter, scheduledAt?: Date | null }. AudienceFilter shape: { countryIds?, categoriesOfInterest?, memberStatuses?, hasActiveFavorites?, registeredAfter?, registeredBefore?, locales?, channelOptIn?: ['chat','applications','promotions','events'] }. Validates with Zod.
   - `campaigns.update`: only when status=DRAFT.
   - `campaigns.estimateAudience`: { audience } → counts users matching filter (without sending) via Prisma query.
   - `campaigns.sendNow` (superAdminProcedure): { id } — resolves audience IDs, dispatches `sendPushToUser` per user (parallel with concurrency 10), updates campaign.sentCount + status='SENT' + sentAt=now. Audit-logged.
   - `campaigns.schedule`: { id, scheduledAt } — sets status=SCHEDULED. (Actual dispatching cron is out of MVP — note in README. For MVP, expose `sendNow` and instruct ops to use it manually; scheduled status is informational.)
   - `campaigns.cancel`: { id } — DRAFT|SCHEDULED → DRAFT.
   - `campaigns.duplicate`: { id }.
   - `triggers.list`: returns the system trigger templates (read-only seed data: 'new_application', 'application_approved', 'new_event_published', 'new_promotion_active', 'new_message_from_admin'). Add `PushTrigger { id, key, titleTemplate, bodyTemplate, isEnabled, channel }` model and seed with defaults.
   - `triggers.update`: { id, patch: { titleTemplate, bodyTemplate, isEnabled } } — editing templates only; keys are fixed.

2. **Page** `apps/admin/src/app/[locale]/(admin)/push/page.tsx`:
   - Tabs: "Кампании" / "Системные триггеры".
   - **Кампании**:
     - DataTable: title, audience summary (count + chips), status, scheduledAt, sentAt, sentCount, kebab.
     - "Создать кампанию" GoldButton → /push/[id] with id="new".
     - Detail page (`push/[id]/page.tsx`): form sections
       - Содержимое: title, body, url (Combobox suggesting common paths: /catalog, /events, etc.).
       - Аудитория: filter builder UI with the AudienceFilter fields. "Оценить аудиторию" button calls estimateAudience → shows count.
       - Время: "Send now" or "Schedule for" datetime.
     - "Отправить сейчас" (SUPER_ADMIN only) opens confirmation with audience count.
   - **Системные триггеры**: simple list with editable templateRow (key, title, body, channel, isEnabled toggle). Variables documented inline: `{{firstName}}`, `{{memberCardId}}`, `{{partnerName}}`, `{{eventTitle}}`, etc.

3. **Trigger integration points** (update existing procedures to fire triggers):
   - `applications.create` → trigger 'new_application' (sent to admins with `applications=true` channel).
   - `applications.approve` → trigger 'application_approved' (sent to the new member).
   - `events.publish` → trigger 'new_event_published' (sent to members in same country/city; respect `events=true` channel).
   - `promotions.setStatus`→ ACTIVE → trigger 'new_promotion_active' (sent to members with active favorites on that partner OR matching categoryOfInterest; respect `promotions=true`).
   - `chat.sendMessage` (admin → member) → trigger 'new_message_from_admin' (Block 23 + 30 already partially wired; here we fully template-ize it).
   - Each trigger renders template with vars, dispatches via sendPushToUser. Fire-and-forget try/catch.

PART B — ONBOARDING EDITOR:
4. **Procedures** in `packages/api/src/routers/admin/onboarding.ts`:
   - `list`: returns all steps (incl. inactive) sorted by displayOrder.
   - `create`: { key (unique), titleRu/En/Uk, descriptionRu/En/Uk, ctaHref, isActive? }. Validates `key` against an allowlist of supported keys: `complete_profile`, `explore_catalog`, `save_first_partner`, `invite_friend`, plus an extensible set. For now, allow any kebab-case key; admin understands it must match the trigger code paths.
   - `update`: { id, patch }.
   - `reorder`: { orderedIds[] }.
   - `toggleActive`: { id }.
   - `delete`: { id } — refuses if any Member has progress recorded on it (data integrity).

5. **Page** `apps/admin/src/app/[locale]/(admin)/onboarding/page.tsx`:
   - Drag-to-reorder list (reuse SortableList from Block 26) of steps: title (RU), key, description (RU short), ctaHref, isActive switch, kebab (Edit, Delete).
   - Dialog form: I18nTabs for title + description, key (read-only on edit), ctaHref input with helper text describing supported keys, isActive switch.
   - Sidebar info box explaining: "Шаги отображаются в виджете до их выполнения. Завершение шага происходит автоматически после действия пользователя — см. документацию."

PART C — REVIEW MODERATION:
6. **Procedures** in `packages/api/src/routers/admin/reviews.ts`:
   - `list`: { cursor?, limit, status?: 'all'|'PENDING'|'APPROVED'|'REJECTED', partnerId?, memberId?, sort?: 'newest'|'oldest'|'lowestRating' }.
   - `get`: full review with deal + partner + member preview.
   - `approve` (auditedAdminProcedure):
     - Sets Review.status='APPROVED', moderatedById=actor, moderatedAt=now.
     - In a transaction: also calls `recomputePartnerRating(partnerId, tx)` (helper from Block 25) to update partner.averageRating + reviewCount.
     - Sends an in-app notification (push trigger 'review_approved' — add to triggers seed: title "Ваш отзыв опубликован", body "{{partnerName}} увидит ваш отзыв в каталоге").
   - `reject` (auditedAdminProcedure): input { id, reason }. Sets REJECTED + saves reason to a new `Review.moderationReason String?` column (add migration). Push 'review_rejected' optional — skip in MVP, just email via `templates/review-rejected.tsx` (short polite explanation with reason).
   - `bulkApprove`: ids[].
   - `bulkReject`: { ids[], reason }.

7. **Page** `apps/admin/src/app/[locale]/(admin)/reviews/page.tsx`:
   - Tabs: Pending / Approved / Rejected.
   - DataTable: partner (logo + name), member (initials + city), rating (5 stars colored), text (first 120 chars + "..."), createdAt, kebab.
   - Row click → side Drawer with full text, deal info, "Approve" / "Reject" actions; reject opens reason textarea.
   - Bulk actions in toolbar.

PART D — AUDIT VIEWER:
8. **Procedures** in `packages/api/src/routers/admin/audit.ts`:
   - `list`: { cursor?, limit, search?, actorId?, entity?, action?, dateFrom?, dateTo? }.
   - `get`: { id } → full row with before/after JSON (pretty-printed).

9. **Page** `apps/admin/src/app/[locale]/(admin)/audit/page.tsx`:
   - DataTable: createdAt, actor (avatar + name), action (badge), entity (chip), entityId (short id with copy button), kebab (View).
   - Filters: action select (autocomplete from distinct values), entity select, actor select, date range, search (matches action or entityId).
   - Row click → opens diff Drawer showing `before` vs `after` JSON side-by-side (use `react-diff-viewer-continued` package or a simple JSON tree). Read-only.

10. **Localization**:
    - `admin.json` for all four sections. Make sure all texts present.

11. **Tests**:
    - Vitest: push campaign estimateAudience returns plausible counts based on seed; sendNow updates sentCount.
    - Vitest: review.approve triggers recomputePartnerRating (mock); partner.averageRating updates correctly.
    - Vitest: onboarding.delete blocked when progress exists.
    - Vitest: audit.list filters correctly by entity/action/date range.
    - Playwright fixme: approve a pending review → public partner page shows the review and updated rating.

ACCEPTANCE CRITERIA:
- /ru/push lists + creates campaigns; estimateAudience returns count; sendNow dispatches.
- System triggers editable; updates propagate to relevant procedures.
- /ru/onboarding drag-reorder + CRUD; member dashboard reflects changes.
- /ru/reviews approve/reject works; partner rating recomputes on approve.
- /ru/audit lists and filters; diff drawer shows before/after.
- Lint + typecheck + Vitest green.

DON'Ts:
- Do not let ADMIN role send push campaigns (SUPER_ADMIN only) — to prevent accidental mass sends.
- Do not skip recomputePartnerRating on review approval — public ratings will drift.
- Do not allow onboarding key duplicates — unique constraint on key.
- Do not show AuditLog entries containing secrets — sanitize `before/after` to redact known secret-bearing field names (e.g., 'hashedPassword', 'twoFactorSecret', 'access_token'). Implement a `sanitizeForAudit(obj)` helper and apply at write time in Block 5 audit middleware too.