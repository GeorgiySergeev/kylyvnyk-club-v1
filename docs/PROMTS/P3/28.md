<!-- Block 28. Placeholder. -->
ROLE: Senior fullstack engineer.

CONTEXT:
- Block 28 of 35. Promotions are time-bounded discounts attached to a partner (Block 15 surfaces active ones publicly).
- Admin must create/edit/schedule promotions, see usage counters, pause/resume, and verify they appear on the public site immediately after activation.

GOAL:
Implement admin.promotions CRUD with scheduling, the list page, and a dedicated form integrating partner search.

REQUIREMENTS:

1. **Implement procedures** in `packages/api/src/routers/admin/promotions.ts`:
   - `list`: { cursor?, limit, search?, partnerId?, status?: 'all'|'DRAFT'|'ACTIVE'|'PAUSED'|'EXPIRED', sort?: 'newest'|'endingSoon'|'biggestDiscount' }.
   - `get`: full row with partner mini-info.
   - `create`: input { partnerId, titleRu/En/Uk, descriptionRu/En/Uk, discountValue, code (uppercase, 4-20 chars, A-Z0-9-), startsAt, endsAt, usageLimit? (null=unlimited), status: 'DRAFT'|'ACTIVE'? (default DRAFT) }.
     - Validates: startsAt < endsAt; partner exists, PUBLISHED, not soft-deleted; code unique per partner (constraint).
   - `update`: { id, patch }. Cannot change partnerId after creation.
   - `setStatus`: { id, status: 'DRAFT'|'ACTIVE'|'PAUSED' } (EXPIRED is automatic via cron later — for MVP, status is set at runtime via `isPromotionActive` helper from Block 15, so the row's status remains ACTIVE even past endsAt; an optional manual "Mark expired" admin action sets status='EXPIRED' to hide cleanly).
   - `delete`: { id } — hard delete (no Favorites/Deals reference promotions). AuditLogged.
   - `duplicate`: { id } — clones a row with status=DRAFT and shifted dates (startsAt=now, endsAt=now+14d), unique code suffix.

2. **List page** `apps/admin/src/app/[locale]/(admin)/promotions/page.tsx`:
   - DataTable columns: Partner (logo + name link), Title (RU primary), Discount (chip), Code, Period (startsAt — endsAt with relative "до 21 мая"), Status (badge: ACTIVE green / PAUSED gold / DRAFT slate / EXPIRED red), Usage (X/Y or X/∞), Kebab.
   - Toolbar: search, status filter, partner filter (Combobox), date range filter (active in window).
   - Row actions: Edit, Pause/Resume, Duplicate, Delete, "Open on site" → /catalog/{slug} (new tab) for the partner.
   - Bulk: Pause / Resume / Delete.
   - Sort defaults: endingSoon for ACTIVE filter; newest otherwise.

3. **Create + Edit page**:
   - `apps/admin/src/app/[locale]/(admin)/promotions/[id]/page.tsx` (id="new" for create).
   - Form sections:
     - **Partner**: Combobox (debounced search via `admin.partners.list`, filters to PUBLISHED). Display selected partner card preview (logo + name + city + current discountValue). Locked after creation (read-only with note).
     - **Содержимое**: I18nTabs with title (required all 3 locales) + description (optional, at least one required).
     - **Скидка**: discountValue (1-100), code (uppercase auto-format; "Сгенерировать" button creates a 6-char alphanumeric like "KYC-AB12CD" — use nanoid alphabet).
     - **Расписание**: startsAt (datetime picker), endsAt (datetime picker), timezone helper (small text "Время в зоне {Intl.TimeZone}" with link to "Use UTC").
     - **Лимиты**: usageLimit (numeric or empty for unlimited).
     - **Статус**: segmented control DRAFT / ACTIVE / PAUSED.
   - Right sidebar (sticky):
     - "Сохранить" button.
     - "Превью на сайте" — small preview using @kyc/ui PromotionCard component.
     - Live countdown to endsAt for sanity check.
     - Метаданные.
   - On save, public catalog/home rails revalidate via revalidateTag('promotions:active').

4. **Code generator helper** in @kyc/api lib:
   - `generatePromoCode(prefix?='KYC-')` returns `${prefix}${nanoid(alphabet='ABCDEFGHJKLMNPQRSTUVWXYZ23456789', size=6)}`. Avoid ambiguous chars (0/O, 1/I).
   - Uniqueness check per partner — retry up to 5x.

5. **Cache invalidation**:
   - Wrap Block 15 `promotions.listActive` in `unstable_cache` tagged `promotions:active`. On any admin mutation, revalidateTag.

6. **Localization**:
   - `admin.json` — labels; reuse `promotions.json` for actual content strings.

7. **Tests**:
   - Vitest: create rejects startsAt ≥ endsAt; duplicate code per partner rejected.
   - Vitest: setStatus transitions allowed/blocked correctly.
   - Vitest: `duplicate` creates a new DRAFT row with unique code.
   - Vitest: code generator produces valid pattern; collisions retried.
   - Playwright fixme: create promotion → activate → appears on /ru "Hot deals"; pause → disappears.

ACCEPTANCE CRITERIA:
- /ru/promotions DataTable + filters + bulk actions.
- Create/edit form with localized content, schedule, partner lock.
- Public site reflects ACTIVE promotions within revalidate window.
- Lint + typecheck + Vitest green.

DON'Ts:
- Do not allow editing partnerId after creation (would invalidate code uniqueness scope).
- Do not surface DRAFT/PAUSED promotions on public site.
- Do not allow usageLimit < usageCount on update — block with clear error.
- Do not skip revalidateTag on status change.